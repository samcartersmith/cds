import { ScaleDensity } from '../types/Scale';

export const defaultSize: Record<ScaleDensity, number> = {
  dense: 40,
  normal: 56,
};

export const compactSize: Record<ScaleDensity, number> = {
  dense: 36,
  normal: 44,
};
