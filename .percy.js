module.exports = {
  version: 2,
  storybook: {
    // Useful for isolating Percy diffs when running from the command line
    // include: ['Core Components/Navigation/Sidebar:*'], // This will only include the sidebar stories
  },
};
