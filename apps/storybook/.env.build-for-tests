# https://storybook.js.org/docs/react/configure/environment-variables
# https://nx.dev/recipe/define-environment-variables

# This env file will be used for the 'build-for-tests' target.
# The 'a11y-web' and 'percy' targets require 'build-for-tests' to be completed before running,
# so they sort of indirectly leverage these env vars bc the majority of their logic is using the output of the 'build-for-tests'.

# If you need to add env vars which are run outside of the build, like in the scripts/run-a11y-tests.ts (aka 'a11y-web' target), then
# you will need to create a .env.a11y-web file and add the env vars there.

STORYBOOK_SKIP_ANIMATION=true