---
title: Deprecations
slug: /resources/deprecations
---

import { Tag } from '@cbhq/cds-web/tag/Tag';
import { AccordionItem } from '@cbhq/cds-web/accordion/AccordionItem';
import { Accordion } from '@cbhq/cds-web/accordion/Accordion';
import { VStack, HStack } from '@cbhq/cds-web/layout';

# Deprecations

Deprecations will be deleted at the end of each quarter in a breaking release. Use the sidebar to navigate to each quarter's deprecations.

We strongly recommend that you use `cds-migrator` to automatically migrate deprecations to their respective replacements. In the case that a migration is not possible, we will provide a manual migration guide in the console. Refer to the [Migration Guide](../../guides/migration) for the appropriate breaking release for more information.
