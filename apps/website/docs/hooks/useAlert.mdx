> [@cbhq/cds-common/overlays/useAlert](https://github.cbhq.net/frontend/cds/blob/master/packages/common/overlays/useAlert.ts)

Consumes `useOverlay()`. Check more info about it in [useOverlay](/hooks/useOverlay).

### Usage

```
const alert = useAlert();

alert.open(<Alert />);
alert.close();
```
