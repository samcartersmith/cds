> [@cbhq/cds-common/hooks/usePreviousValue](https://github.cbhq.net/frontend/cds/blob/master/packages/common/hooks/usePreviousValue.ts)

Returns the previous value of a variable. Useful for detecting changes.

```jsx
const previousValue = usePreviousValue(value);
```
