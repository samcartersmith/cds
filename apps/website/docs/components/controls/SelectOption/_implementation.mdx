import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessory

<ApiDetails
  componentName="SelectOption"
  propertyName="accessory"
  required={false}
  defaultValue=""
  mobileOptions={['ReactElement<CellAccessoryProps, string | JSXElementConstructor<any>>']}
  webOptions={['ReactElement<CellAccessoryProps, string | JSXElementConstructor<any>>']}
/>

### alignItems

<ApiDetails
  componentName="SelectOption"
  propertyName="alignItems"
  required={false}
  defaultValue=""
  mobileOptions={['center', 'flex-end', 'flex-start']}
  webOptions={['center', 'flex-end', 'flex-start']}
/>

### borderRadius

<ApiDetails
  componentName="SelectOption"
  propertyName="borderRadius"
  required={false}
  defaultValue=""
  mobileOptions={[
    'badge',
    'compact',
    'input',
    'none',
    'pill',
    'popover',
    'round',
    'search',
    'standard',
    'tooltip',
    'tooltipV2',
  ]}
  webOptions={[
    'badge',
    'compact',
    'input',
    'none',
    'pill',
    'popover',
    'round',
    'search',
    'standard',
    'tooltip',
    'tooltipV2',
  ]}
/>

### compact <Badge variant="web" />

enables compact spacing

<ApiDetails
  componentName="SelectOption"
  propertyName="compact"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### description

Description of content. Max 1 line (with title) or 2 lines (without), otherwise will truncate.

<ApiDetails
  componentName="SelectOption"
  propertyName="description"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### detail

<ApiDetails
  componentName="SelectOption"
  propertyName="detail"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### detailWidth

Apply a fixed width to the detail (end).

<ApiDetails
  componentName="SelectOption"
  propertyName="detailWidth"
  required={false}
  defaultValue=""
  mobileOptions={['number', 'string']}
  webOptions={['number', 'string']}
/>

### disableCloseOnOptionChange <Badge variant="web" />

Prevent menu from closing when an option is selected

<ApiDetails
  componentName="SelectOption"
  propertyName="disableCloseOnOptionChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### disabled

Is the cell disabled? Will apply opacity and disable interaction.

<ApiDetails
  componentName="SelectOption"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### innerSpacing

The spacing to use on the inner content of Cell

<ApiDetails
  componentName="SelectOption"
  propertyName="innerSpacing"
  required={false}
  defaultValue=""
  mobileOptions={['CellSpacing']}
  webOptions={['CellSpacing']}
/>

### intermediary

<ApiDetails
  componentName="SelectOption"
  propertyName="intermediary"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### maxHeight

<ApiDetails
  componentName="SelectOption"
  propertyName="maxHeight"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>

### media

<ApiDetails
  componentName="SelectOption"
  propertyName="media"
  required={false}
  defaultValue=""
  mobileOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

### minHeight

<ApiDetails
  componentName="SelectOption"
  propertyName="minHeight"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>

### multiline

Allow the description to span multiple lines. This _will_ break fixed height requirements, so should not be used in a `FlatList`.

<ApiDetails
  componentName="SelectOption"
  propertyName="multiline"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### onKeyPress <Badge variant="web" />

Callback fired when a key is pressed

<ApiDetails
  componentName="SelectOption"
  propertyName="onKeyPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['KeyboardEventHandler<Element>']}
/>

### onPress

Callback fired when the element is pressed.

<ApiDetails
  componentName="SelectOption"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void', 'NoopFn']}
  webOptions={['OnPress']}
/>

### outerSpacing

The spacing to use on the parent wrapper of Cell

<ApiDetails
  componentName="SelectOption"
  propertyName="outerSpacing"
  required={false}
  defaultValue=""
  mobileOptions={['CellSpacing']}
  webOptions={['CellSpacing']}
/>

### priority

Which piece of content has the highest priority in regards to text truncation, growing, and shrinking.

<ApiDetails
  componentName="SelectOption"
  propertyName="priority"
  required={false}
  defaultValue=""
  mobileOptions={['(end | start | middle)[]', 'end', 'middle', 'start']}
  webOptions={['(start | end | middle)[]', 'end', 'middle', 'start']}
/>

### shouldOverflow

<ApiDetails
  componentName="SelectOption"
  propertyName="shouldOverflow"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### tabIndex <Badge variant="web" />

Necessary to control roving tabindex for accessibility https://www.w3.org/TR/wai-aria-practices/#kbd_roving_tabindex

<ApiDetails
  componentName="SelectOption"
  propertyName="tabIndex"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['number']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="SelectOption"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### title

Title of content. Max 1 line (with description) or 2 lines (without), otherwise will truncate.

<ApiDetails
  componentName="SelectOption"
  propertyName="title"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### to <Badge variant="web" />

URL that this links to when pressed.

<ApiDetails
  componentName="SelectOption"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### value

Unique identifier for each option

<ApiDetails
  componentName="SelectOption"
  propertyName="value"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>
