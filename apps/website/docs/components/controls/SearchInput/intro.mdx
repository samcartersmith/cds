import { Box } from '@cbhq/cds-web/layout/Box';
import { SearchInputSheet } from './SearchInputSheet';

Search allows users to quickly find content within or across an application. Inputting a string of text into an input filters results from a dataset.

<SearchInputSheet />