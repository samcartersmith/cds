import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### aria-labelledby <Badge variant="web" />

id of the element that labels the radio group Identifies the element (or elements) that labels the current element. @see aria-describedby.

<ApiDetails
  componentName="RadioGroup"
  propertyName="aria-labelledby"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### label

Set a label summary for the group of radios.

<ApiDetails
  componentName="RadioGroup"
  propertyName="label"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### name <Badge variant="web" />

- Field name of the multiple choice radio group.

<ApiDetails
  componentName="RadioGroup"
  propertyName="name"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### onChange

Handle change event when pressing on a radio option.

<ApiDetails
  componentName="RadioGroup"
  propertyName="onChange"
  required={false}
  defaultValue=""
  mobileOptions={['(value?: T | undefined) => void']}
  webOptions={['(value: T) => void']}
/>

### options

Multiple choice options for the radio group. The object key represents the radio input value and the object value represents the radio option label.

<ApiDetails
  componentName="RadioGroup"
  propertyName="options"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### selectedValue

Currently selected value.

<ApiDetails
  componentName="RadioGroup"
  propertyName="selectedValue"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="RadioGroup"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>
