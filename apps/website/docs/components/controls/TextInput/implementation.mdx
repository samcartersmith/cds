import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/controls/TextInput](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/controls/TextInput.tsx)

<ComponentDetails componentName="TextInput" templateName="intro" />

## Usage

<ComponentDetails componentName="TextInput" templateName="usage" />

## Accessibility

<ComponentDetails componentName="TextInput" templateName="a11y" />

## Props

### accessibilityHint

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="TextInput"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-accessibilityHint" />

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="TextInput"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-accessibilityLabel" />

### accessibilityLabelledBy

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="TextInput"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-accessibilityLabelledBy" />

### align

Aligns text inside input and helperText

<ApiDetails
  componentName="TextInput"
  propertyName="align"
  required={false}
  defaultValue="start"
  mobileOptions={['center', 'end', 'justify', 'start']}
  webOptions={['center', 'end', 'justify', 'start']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-align" />

### bordered <Badge variant="web" />

Adds border to input

<ApiDetails
  componentName="TextInput"
  propertyName="bordered"
  required={false}
  defaultValue="true"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-bordered" />

### borderRadius

Leverage one of the borderRadius styles we offer to round the corners of the input.

<ApiDetails
  componentName="TextInput"
  propertyName="borderRadius"
  required={false}
  defaultValue="input"
  mobileOptions={['input', 'search']}
  webOptions={['input', 'search']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-borderRadius" />

### compact

Enables compact variation

<ApiDetails
  componentName="TextInput"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-compact" />

### disabled

Toggles input interactability and opacity

<ApiDetails
  componentName="TextInput"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-disabled" />

### end

Adds content to the end of the inner input. Refer to diagram for location of endNode in InputStack component

<ApiDetails
  componentName="TextInput"
  propertyName="end"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="TextInput" templateName="prop-end" />

### helperText

For cases where label is not enough information to describe what the text input is for. Can also be used for showing positive/negative messages

<ApiDetails
  componentName="TextInput"
  propertyName="helperText"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-helperText" />

### inputNode <Badge variant="web" /> <Badge variant="danger" order={2} />

Customize the element which the input area will be rendered as. Adds ability to render the input area as a `<textarea />`, `<input />` etc... By default, the input area will be rendered as an `<input />`. Our default input handles all of the UI/Accessibility needs for your out of the box, but inputNode will not include those.
Use this at your own risk, and don't use unless ABSOLUTELY NECESSARY. You may see weird UI when focusing etc..

<ApiDetails
  componentName="TextInput"
  propertyName="inputNode"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-inputNode" />

### label

Short messageArea indicating purpose of input

<ApiDetails
  componentName="TextInput"
  propertyName="label"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-label" />

### onChange

<ApiDetails
  componentName="TextInput"
  propertyName="onChange"
  required={false}
  defaultValue=""
  mobileOptions={['(e: NativeSyntheticEvent<TextInputChangeEventData>) => void']}
  webOptions={['ChangeEventHandler<HTMLInputElement>']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-onChange" />

### onChangeText <Badge variant="mobile" />

<ApiDetails
  componentName="TextInput"
  propertyName="onChangeText"
  required={false}
  defaultValue=""
  mobileOptions={['(text: string) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="TextInput" templateName="prop-onChangeText" />

### onPress <Badge variant="web" />

Callback fired when pressed/clicked

<ApiDetails
  componentName="TextInput"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-onPress" />

### placeholder

Placeholder text displayed inside of the input. Will be replaced if there is a value.

<ApiDetails
  componentName="TextInput"
  propertyName="placeholder"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-placeholder" />

### start

Adds content to the start of the inner input. Refer to diagram for location of startNode in InputStack component

<ApiDetails
  componentName="TextInput"
  propertyName="start"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="TextInput" templateName="prop-start" />

### suffix

Adds suffix text to the end of input

<ApiDetails
  componentName="TextInput"
  propertyName="suffix"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-suffix" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="TextInput"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-testID" />

### testIDMap

Add ability to test individual parts of the input

<ApiDetails
  componentName="TextInput"
  propertyName="testIDMap"
  required={false}
  defaultValue=""
  mobileOptions={[
    '{ input?: string | undefined; start?: string | undefined; end?: string | undefined; label?: string | undefined; helperText?: string | undefined; }',
  ]}
  webOptions={[
    '{ input?: string | undefined; start?: string | undefined; end?: string | undefined; label?: string | undefined; helperText?: string | undefined; }',
  ]}
/>

<ComponentDetails componentName="TextInput" templateName="prop-testIDMap" />

### value

<ApiDetails
  componentName="TextInput"
  propertyName="value"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['number', 'readonly string[]', 'string']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-value" />

### variant

Determines the sentiment of the input. Because we allow startContent and endContent to be custom ReactNode, the content placed inside these slots will not change colors according to the variant. You will have to add that yourself

<ApiDetails
  componentName="TextInput"
  propertyName="variant"
  required={false}
  defaultValue="foregroundMuted"
  mobileOptions={['foreground', 'foregroundMuted', 'negative', 'positive', 'primary']}
  webOptions={['foreground', 'foregroundMuted', 'negative', 'positive', 'primary']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-variant" />

### width

Width of input as a percentage string.

<ApiDetails
  componentName="TextInput"
  propertyName="width"
  required={false}
  defaultValue="100%"
  mobileOptions={['`${number}%`', 'number']}
  webOptions={['`${number}%`', 'number']}
/>

<ComponentDetails componentName="TextInput" templateName="prop-width" />

<ComponentDetails componentName="TextInput" templateName="outro" />
