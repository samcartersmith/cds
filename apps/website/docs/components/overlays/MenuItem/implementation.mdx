import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/overlays/MenuItem](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/overlays/MenuItem.tsx)

<ComponentDetails componentName="MenuItem" templateName="intro" />

## Usage

<ComponentDetails componentName="MenuItem" templateName="usage" />

## Accessibility

<ComponentDetails componentName="MenuItem" templateName="a11y" />

## Props

### accessibilityDescriptionId <Badge variant="web" />

Used to associate descriptive text to an input for accessibility

<ApiDetails
  componentName="MenuItem"
  propertyName="accessibilityDescriptionId"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-accessibilityDescriptionId" />

### accessibilityHint <Badge variant="web" />

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="MenuItem"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-accessibilityHint" />

### accessibilityLabel <Badge variant="web" />

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="MenuItem"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-accessibilityLabel" />

### accessibilityLabelId <Badge variant="web" />

Used to associate a label with an input for accessibility

<ApiDetails
  componentName="MenuItem"
  propertyName="accessibilityLabelId"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-accessibilityLabelId" />

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="MenuItem"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-accessibilityLabelledBy" />

### disableCloseOnOptionChange <Badge variant="web" />

Prevent menu from closing when an option is selected

<ApiDetails
  componentName="MenuItem"
  propertyName="disableCloseOnOptionChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-disableCloseOnOptionChange" />

### loading <Badge variant="web" />

Is the element currenty loading.

<ApiDetails
  componentName="MenuItem"
  propertyName="loading"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-loading" />

### onKeyPress <Badge variant="web" />

Callback fired when a key is pressed

<ApiDetails
  componentName="MenuItem"
  propertyName="onKeyPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['KeyboardEventHandler<Element>']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-onKeyPress" />

### onPress <Badge variant="web" />

Custom event handler for when a MenuItem is pressed Callback fired when the element is pressed.

<ApiDetails
  componentName="MenuItem"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['NoopFn & MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-onPress" />

### tabIndex <Badge variant="web" />

Necessary to control roving tabindex for accessibility https://www.w3.org/TR/wai-aria-practices/#kbd_roving_tabindex

<ApiDetails
  componentName="MenuItem"
  propertyName="tabIndex"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['number']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-tabIndex" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="MenuItem"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-testID" />

### to <Badge variant="web" />

URL that this links to when pressed.

<ApiDetails
  componentName="MenuItem"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-to" />

### value <Badge variant="web" />

automatically focus a select option if its already been selected

<ApiDetails
  componentName="MenuItem"
  propertyName="value"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="MenuItem" templateName="prop-value" />

<ComponentDetails componentName="MenuItem" templateName="outro" />
