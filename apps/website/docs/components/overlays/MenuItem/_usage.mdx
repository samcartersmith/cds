### Web

`MenuItem` is a Higher Order Component used to wrap interactables. We recommend using `SelectOption` whenever possible, but sometimes there are cases where you may need to pass a custom interactable to a `PopoverMenu`.

When used with `Select` or `PopoverMenu` it injects keyboard navigation, accessibility properties, and focus behaviors.
