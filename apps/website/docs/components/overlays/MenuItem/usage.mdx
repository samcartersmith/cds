### Web

`MenuItem` is a Higher Order Component used to wrap non-interactables. When used with `Select` or `PopoverMenu` it injects keyboard navigation, accessibility properties, and focus behaviors.
