import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### action

Optional toast action

<ApiDetails
  componentName="Toast"
  propertyName="action"
  required={false}
  defaultValue=""
  mobileOptions={['ToastAction']}
  webOptions={['ToastAction']}
/>

### bottomOffset

Bottom offset postion

<ApiDetails
  componentName="Toast"
  propertyName="bottomOffset"
  required={false}
  defaultValue=""
  mobileOptions={['number', 'string']}
  webOptions={['number', 'string']}
/>

### disablePortal <Badge variant="web" />

Disable React portal integration

<ApiDetails
  componentName="Toast"
  propertyName="disablePortal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### hideCloseButton <Badge variant="web" />

Hide the close button on the right

<ApiDetails
  componentName="Toast"
  propertyName="hideCloseButton"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### onDidHide

Callback fired after the component is hidden.

<ApiDetails
  componentName="Toast"
  propertyName="onDidHide"
  required={false}
  defaultValue=""
  mobileOptions={['NoopFn']}
  webOptions={['NoopFn']}
/>

### onWillHide

Callback fired before the component hides.

<ApiDetails
  componentName="Toast"
  propertyName="onWillHide"
  required={false}
  defaultValue=""
  mobileOptions={['NoopFn']}
  webOptions={['NoopFn']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Toast"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### text

<ApiDetails
  componentName="Toast"
  propertyName="text"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>
