import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### primaryAction

Primary action button

<ApiDetails
  componentName="ModalFooter"
  propertyName="primaryAction"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### secondaryAction

Secondary action button

<ApiDetails
  componentName="ModalFooter"
  propertyName="secondaryAction"
  required={false}
  defaultValue=""
  mobileOptions={['ReactElement<ButtonBaseProps, string | JSXElementConstructor<any>>']}
  webOptions={['ReactElement<ButtonBaseProps, string | JSXElementConstructor<any>>']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="ModalFooter"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### vertical <Badge variant="mobile" />

Stack buttons vertically instead of horizontally.

<ApiDetails
  componentName="ModalFooter"
  propertyName="vertical"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>
