import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessibilityLabel <Badge variant="web" />

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="FullscreenModal"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="FullscreenModal"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### dangerouslySetContentClassName <Badge variant="web" /> <Badge variant="danger" order={2} />

This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="dangerouslySetContentClassName"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### disablePortal <Badge variant="web" />

Disable React portal integration

<ApiDetails
  componentName="FullscreenModal"
  propertyName="disablePortal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### hideDivider <Badge variant="web" />

Hide header bottom divider

<ApiDetails
  componentName="FullscreenModal"
  propertyName="hideDivider"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### onRequestClose <Badge variant="web" />

Callback function fired when modal is closed.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="onRequestClose"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### primaryContent <Badge variant="web" />

Primary content element. Primary content is where the core of the task or information should live.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="primaryContent"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### role <Badge variant="web" />

WAI-ARIA Roles

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles**

<ApiDetails
  componentName="FullscreenModal"
  propertyName="role"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['alertdialog', 'dialog']}
/>

### secondaryContent <Badge variant="web" />

Secondary content element. Secondary content is supplemental information.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="secondaryContent"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="FullscreenModal"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### title <Badge variant="web" />

Title displayed in the Fullscreen Modal header.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="title"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### visible <Badge variant="web" />

Controls visibility of the Modal

<ApiDetails
  componentName="FullscreenModal"
  propertyName="visible"
  required={true}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### zIndex <Badge variant="web" />

Adjust the z-index positioning layer.

<ApiDetails
  componentName="FullscreenModal"
  propertyName="zIndex"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['number']}
/>
