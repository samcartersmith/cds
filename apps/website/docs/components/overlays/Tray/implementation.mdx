import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-mobile/overlays/Tray](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/mobile/overlays/Tray.tsx)

<ComponentDetails componentName="Tray" templateName="intro" />

## Usage

<ComponentDetails componentName="Tray" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Tray" templateName="a11y" />

## Props

### disableCapturePanGestureToDismiss <Badge variant="mobile" />

Prevents the Drawer from capturing pan gestures on children. Set to true when using a ScrollView as a child

<ApiDetails
  componentName="Tray"
  propertyName="disableCapturePanGestureToDismiss"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-disableCapturePanGestureToDismiss" />

### hideHandleBar <Badge variant="mobile" />

The HandleBar by default only is used for a bottom pinned drawer. This removes it.

<ApiDetails
  componentName="Tray"
  propertyName="hideHandleBar"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-hideHandleBar" />

### onCloseComplete <Badge variant="mobile" />

Action that will happen when drawer is dismissed

<ApiDetails
  componentName="Tray"
  propertyName="onCloseComplete"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-onCloseComplete" />

### onVisibilityChange <Badge variant="mobile" />

Optional callback that, if provided, will be triggered when the Tray is toggled open/ closed If used for analytics, context (visible | hidden) can be bundled with the event info to track whether the multiselect was toggled into or out of view

<ApiDetails
  componentName="Tray"
  propertyName="onVisibilityChange"
  required={false}
  defaultValue=""
  mobileOptions={['(context: visible | hidden) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-onVisibilityChange" />

### preventDismissGestures <Badge variant="mobile" />

Prevents a user from dismissing the drawer by pressing the overlay or swiping

<ApiDetails
  componentName="Tray"
  propertyName="preventDismissGestures"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-preventDismissGestures" />

### testID <Badge variant="mobile" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Tray"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-testID" />

### title <Badge variant="mobile" />

Optional Tray title, text only

<ApiDetails
  componentName="Tray"
  propertyName="title"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={[]}
/>

<ComponentDetails componentName="Tray" templateName="prop-title" />

<ComponentDetails componentName="Tray" templateName="outro" />
