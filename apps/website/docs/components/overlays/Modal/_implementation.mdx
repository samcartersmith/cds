import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="Modal"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="Modal"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### children

Component to render as the Modal content

<ApiDetails
  componentName="Modal"
  propertyName="children"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'Iterable<ReactNode> & ReactElement<any, string | JSXElementConstructor<any>>',
    'Iterable<ReactNode> & ReactPortal',
    'Iterable<ReactNode> & false',
    'Iterable<ReactNode> & number',
    'Iterable<ReactNode> & string',
    'Iterable<ReactNode> & true',
    'ModalRenderChildren',
    'ModalRenderChildren & Iterable<ReactNode>',
    'ModalRenderChildren & ReactElement<any, string | JSXElementConstructor<any>>',
    'ModalRenderChildren & ReactPortal',
    'ModalRenderChildren & false',
    'ModalRenderChildren & number',
    'ModalRenderChildren & string',
    'ModalRenderChildren & true',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactElement<any, string | JSXElementConstructor<any>> & Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>> & ReactPortal',
    'ReactElement<any, string | JSXElementConstructor<any>> & false',
    'ReactElement<any, string | JSXElementConstructor<any>> & number',
    'ReactElement<any, string | JSXElementConstructor<any>> & string',
    'ReactElement<any, string | JSXElementConstructor<any>> & true',
    'ReactPortal',
    'ReactPortal & Iterable<ReactNode>',
    'ReactPortal & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal & false',
    'ReactPortal & number',
    'ReactPortal & string',
    'ReactPortal & true',
    'false',
    'false & Iterable<ReactNode>',
    'false & ReactElement<any, string | JSXElementConstructor<any>>',
    'false & ReactPortal',
    'false & {}',
    'number',
    'number & Iterable<ReactNode>',
    'number & ReactElement<any, string | JSXElementConstructor<any>>',
    'number & ReactPortal',
    'number & {}',
    'string',
    'string & Iterable<ReactNode>',
    'string & ReactElement<any, string | JSXElementConstructor<any>>',
    'string & ReactPortal',
    'string & {}',
    'true',
    'true & Iterable<ReactNode>',
    'true & ReactElement<any, string | JSXElementConstructor<any>>',
    'true & ReactPortal',
    'true & {}',
    '{}',
    '{} & false',
    '{} & number',
    '{} & string',
    '{} & true',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'Iterable<ReactNode> & ReactElement<any, string | JSXElementConstructor<any>>',
    'Iterable<ReactNode> & ReactPortal',
    'Iterable<ReactNode> & false',
    'Iterable<ReactNode> & number',
    'Iterable<ReactNode> & string',
    'Iterable<ReactNode> & true',
    'ModalRenderChildren',
    'ModalRenderChildren & Iterable<ReactNode>',
    'ModalRenderChildren & ReactElement<any, string | JSXElementConstructor<any>>',
    'ModalRenderChildren & ReactPortal',
    'ModalRenderChildren & false',
    'ModalRenderChildren & number',
    'ModalRenderChildren & string',
    'ModalRenderChildren & true',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactElement<any, string | JSXElementConstructor<any>> & Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>> & ReactPortal',
    'ReactElement<any, string | JSXElementConstructor<any>> & false',
    'ReactElement<any, string | JSXElementConstructor<any>> & number',
    'ReactElement<any, string | JSXElementConstructor<any>> & string',
    'ReactElement<any, string | JSXElementConstructor<any>> & true',
    'ReactPortal',
    'ReactPortal & Iterable<ReactNode>',
    'ReactPortal & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal & false',
    'ReactPortal & number',
    'ReactPortal & string',
    'ReactPortal & true',
    'false',
    'false & Iterable<ReactNode>',
    'false & ReactElement<any, string | JSXElementConstructor<any>>',
    'false & ReactPortal',
    'false & {}',
    'number',
    'number & Iterable<ReactNode>',
    'number & ReactElement<any, string | JSXElementConstructor<any>>',
    'number & ReactPortal',
    'number & {}',
    'string',
    'string & Iterable<ReactNode>',
    'string & ReactElement<any, string | JSXElementConstructor<any>>',
    'string & ReactPortal',
    'string & {}',
    'true',
    'true & Iterable<ReactNode>',
    'true & ReactElement<any, string | JSXElementConstructor<any>>',
    'true & ReactPortal',
    'true & {}',
    '{}',
    '{} & false',
    '{} & number',
    '{} & string',
    '{} & true',
  ]}
/>

### dangerouslyDisableResponsiveness <Badge variant="web" /> <Badge variant="danger" order={2} />

Disable responsiveness so it maintains the same UI across different viewports.
This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="Modal"
  propertyName="dangerouslyDisableResponsiveness"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### dangerouslySetClassName <Badge variant="web" /> <Badge variant="danger" order={2} />

This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="Modal"
  propertyName="dangerouslySetClassName"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### dangerouslySetPosition <Badge variant="web" /> <Badge variant="danger" order={2} />

Set the position for the modal dialogue
This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="Modal"
  propertyName="dangerouslySetPosition"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['absolute', 'fixed', 'relative', 'static', 'sticky']}
/>

### dangerouslySetWidth <Badge variant="danger" order={1} />

This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="Modal"
  propertyName="dangerouslySetWidth"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>

### disableOverlayPress <Badge variant="web" />

Disable overlay click that closes the Modal

<ApiDetails
  componentName="Modal"
  propertyName="disableOverlayPress"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### disablePortal <Badge variant="web" />

Disable React portal integration

<ApiDetails
  componentName="Modal"
  propertyName="disablePortal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### hideCloseButton

Hide the close icon on the top right

<ApiDetails
  componentName="Modal"
  propertyName="hideCloseButton"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### hideDividers

Hide top and bottom dividers inside Modal body

<ApiDetails
  componentName="Modal"
  propertyName="hideDividers"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### id <Badge variant="web" />

Used for a11y to indicate the relationship between a controlled element and its controller eg: a Button that controls the visibility of a Modal

<ApiDetails
  componentName="Modal"
  propertyName="id"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### onDidClose

Callback fired after the component is closed.

<ApiDetails
  componentName="Modal"
  propertyName="onDidClose"
  required={false}
  defaultValue=""
  mobileOptions={['NoopFn']}
  webOptions={['NoopFn']}
/>

### onRequestClose

Callback function fired when modal is closed.

<ApiDetails
  componentName="Modal"
  propertyName="onRequestClose"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### shouldCloseOnEscPress <Badge variant="web" />

If pressing the esc key should close the modal

<ApiDetails
  componentName="Modal"
  propertyName="shouldCloseOnEscPress"
  required={false}
  defaultValue="true"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Modal"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### visible

Controls visibility of the Modal

<ApiDetails
  componentName="Modal"
  propertyName="visible"
  required={true}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### zIndex

Adjust the z-index positioning layer.

<ApiDetails
  componentName="Modal"
  propertyName="zIndex"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>
