import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### children <Badge variant="mobile" />

Component to render as the Modal content

<ApiDetails
  componentName="Drawer"
  propertyName="children"
  required={false}
  defaultValue=""
  mobileOptions={[
    'DrawerRenderChildren',
    'DrawerRenderChildren & Iterable<ReactNode>',
    'DrawerRenderChildren & ReactElement<any, string | JSXElementConstructor<any>>',
    'DrawerRenderChildren & ReactPortal',
    'DrawerRenderChildren & false',
    'DrawerRenderChildren & number',
    'DrawerRenderChildren & string',
    'DrawerRenderChildren & true',
    'Iterable<ReactNode>',
    'Iterable<ReactNode> & ReactElement<any, string | JSXElementConstructor<any>>',
    'Iterable<ReactNode> & ReactPortal',
    'Iterable<ReactNode> & false',
    'Iterable<ReactNode> & number',
    'Iterable<ReactNode> & string',
    'Iterable<ReactNode> & true',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactElement<any, string | JSXElementConstructor<any>> & Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>> & ReactPortal',
    'ReactElement<any, string | JSXElementConstructor<any>> & false',
    'ReactElement<any, string | JSXElementConstructor<any>> & number',
    'ReactElement<any, string | JSXElementConstructor<any>> & string',
    'ReactElement<any, string | JSXElementConstructor<any>> & true',
    'ReactPortal',
    'ReactPortal & Iterable<ReactNode>',
    'ReactPortal & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal & false',
    'ReactPortal & number',
    'ReactPortal & string',
    'ReactPortal & true',
    'false',
    'false & Iterable<ReactNode>',
    'false & ReactElement<any, string | JSXElementConstructor<any>>',
    'false & ReactPortal',
    'false & {}',
    'number',
    'number & Iterable<ReactNode>',
    'number & ReactElement<any, string | JSXElementConstructor<any>>',
    'number & ReactPortal',
    'number & {}',
    'string',
    'string & Iterable<ReactNode>',
    'string & ReactElement<any, string | JSXElementConstructor<any>>',
    'string & ReactPortal',
    'string & {}',
    'true',
    'true & Iterable<ReactNode>',
    'true & ReactElement<any, string | JSXElementConstructor<any>>',
    'true & ReactPortal',
    'true & {}',
    '{}',
    '{} & false',
    '{} & number',
    '{} & string',
    '{} & true',
  ]}
  webOptions={[]}
/>

### disableCapturePanGestureToDismiss <Badge variant="mobile" />

Prevents the Drawer from capturing pan gestures on children. Set to true when using a ScrollView as a child

<ApiDetails
  componentName="Drawer"
  propertyName="disableCapturePanGestureToDismiss"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

### hideHandleBar <Badge variant="mobile" />

The HandleBar by default only is used for a bottom pinned drawer. This removes it.

<ApiDetails
  componentName="Drawer"
  propertyName="hideHandleBar"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

### onBlur <Badge variant="mobile" />

Callback fired when the overlay is pressed, or swipe to close

<ApiDetails
  componentName="Drawer"
  propertyName="onBlur"
  required={false}
  defaultValue=""
  mobileOptions={['NoopFn']}
  webOptions={[]}
/>

### onCloseComplete <Badge variant="mobile" />

Action that will happen when drawer is dismissed

<ApiDetails
  componentName="Drawer"
  propertyName="onCloseComplete"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### pin <Badge variant="mobile" />

Pin the modal to one side of the screen

<ApiDetails
  componentName="Drawer"
  propertyName="pin"
  required={true}
  defaultValue="bottom"
  mobileOptions={['all', 'bottom', 'left', 'right', 'top']}
  webOptions={[]}
/>

### preventDismissGestures <Badge variant="mobile" />

Prevents a user from dismissing the drawer by pressing the overlay or swiping

<ApiDetails
  componentName="Drawer"
  propertyName="preventDismissGestures"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

### testID <Badge variant="mobile" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID Used to locate this view in end-to-end tests.

<ApiDetails
  componentName="Drawer"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={[]}
/>
