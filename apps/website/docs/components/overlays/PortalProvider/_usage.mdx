### Basic

```jsx
<PortalProvider>
  <App />
</PortalProvider>
```

### Toast Bottom Offset

You can set `toastBottomOffset` to have all Toasts inside the provider displayed above its original position.

```jsx
<PortalProvider toastBottomOffset={30}>
  <App />
</PortalProvider>
```
