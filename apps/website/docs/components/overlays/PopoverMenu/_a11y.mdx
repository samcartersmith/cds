`PopoverMenu`s come with baked in accessibility attributes for both the component passed as a child to `<PopoverTrigger>` and the elevated surface that is toggled by interacting with the trigger. To ensure the best user experience, we advise that you pass an `accessibilityLabel` to `PopoverMenu` to ensure that all `PopoverMenu`s have unique and indicative labels.
