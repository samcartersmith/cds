import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessibilityLabel <Badge variant="web" />

Full length accessibility label when the child text is not descriptive enough.

<ApiDetails
  componentName="NavLink"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### active <Badge variant="web" />

<ApiDetails
  componentName="NavLink"
  propertyName="active"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### children <Badge variant="web" />

Children to render within the link.

<ApiDetails
  componentName="NavLink"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### dangerouslySetClassName <Badge variant="web" /> <Badge variant="danger" order={2} />

Adds a className to the Link. If you pass in a className make sure your styles override the Link styles using the Link class .cds-link like this: .my-class.cds-link

<ApiDetails
  componentName="NavLink"
  propertyName="dangerouslySetClassName"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### mono <Badge variant="web" />

Use CoinbaseMono font

<ApiDetails
  componentName="NavLink"
  propertyName="mono"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### onPress <Badge variant="web" />

Callback fired when the element is clicked.

<ApiDetails
  componentName="NavLink"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['OnPress<HTMLAnchorElement>']}
/>

### openInNewWindow <Badge variant="web" />

If true, it opens the link in a new window. If false, it replaces the current screen with the link

<ApiDetails
  componentName="NavLink"
  propertyName="openInNewWindow"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### renderContainer <Badge variant="web" />

Callback for custom Link component

<ApiDetails
  componentName="NavLink"
  propertyName="renderContainer"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['(props: HTMLAttributes<HTMLAnchorElement>) => Element']}
/>

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="NavLink"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### to <Badge variant="web" />

URL that this link goes to when pressed.

<ApiDetails
  componentName="NavLink"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>
