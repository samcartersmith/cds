import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### autoCollapse <Badge variant="web" />

When set, the sidebar will auto collapse at or below the tablet breakpoint (currently 768px)

<ApiDetails
  componentName="Sidebar"
  propertyName="autoCollapse"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### children <Badge variant="web" />

Children are expected to be an array of SidebarItems

<ApiDetails
  componentName="Sidebar"
  propertyName="children"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[
    'ReactNode[]',
    'ReactNode[] & Iterable<ReactNode>',
    'ReactNode[] & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNode[] & ReactPortal',
    'ReactNode[] & false',
    'ReactNode[] & number',
    'ReactNode[] & string',
    'ReactNode[] & true',
  ]}
/>

### collapsed <Badge variant="web" />

Use collapsed to show only the logo

<ApiDetails
  componentName="Sidebar"
  propertyName="collapsed"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### logo <Badge variant="web" />

The logo to display

<ApiDetails
  componentName="Sidebar"
  propertyName="logo"
  required={true}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[]}
/>

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Sidebar"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>
