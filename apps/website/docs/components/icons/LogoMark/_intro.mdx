import { AddRemoteImageBanner } from ':cds-website/shared/announcements/AddRemoteImageBanner';

<AddRemoteImageBanner />

The mark comes in 3 sizes 16, 24, 32 and 2 variants light and dark, which correspond to whichever spectrum is being displayed. The mark cannot go below 16px, but beyond 32px it’ll scale up indefinitely.
