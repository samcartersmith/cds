import { IconBaseSheet } from '../IconBaseSheet';
import { IconAnnouncement } from '../IconAnnouncement';

<IconAnnouncement />

<span>&nbsp;</span>

Icons are hand crafted symbols for common visual actions and items. 

You now have the ability to search for an icon using keywords recommended by CDS Illustrators. For example, if you search "history", it will recommend recurring icon. This is because our designers believe that if you want an icon to represent history, recurring would be a good icon candidate.

<IconBaseSheet category="product" />
