import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### alt <Badge variant="web" />

<ApiDetails
  componentName="RemoteImage"
  propertyName="alt"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### animated <Badge variant="mobile" />

If value is `true` then style prop can take animated values.

<ApiDetails
  componentName="RemoteImage"
  propertyName="animated"
  required={false}
  defaultValue="false"
  mobileOptions={['false']}
  webOptions={[]}
/>

### aspectRatio

<ApiDetails
  componentName="RemoteImage"
  propertyName="aspectRatio"
  required={false}
  defaultValue=""
  mobileOptions={[
    '[number, number]',
    '[number, number] & readonly [number, number]',
    'readonly [number, number]',
    'readonly [number, number] & [number, number]',
  ]}
  webOptions={[
    '[number, number]',
    '[number, number] & readonly [number, number]',
    'readonly [number, number]',
    'readonly [number, number] & [number, number]',
  ]}
/>

### dangerouslySetClassName <Badge variant="web" />

<ApiDetails
  componentName="RemoteImage"
  propertyName="dangerouslySetClassName"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### dangerouslySetStyle <Badge variant="mobile" /> <Badge variant="danger" order={2} />

This is a migration escape hatch. It is not intended to be used normally.

<ApiDetails
  componentName="RemoteImage"
  propertyName="dangerouslySetStyle"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ImageStyle',
    'RecursiveArray<Falsy | ImageStyle | RegisteredStyle<ImageStyle>>',
    'RegisteredStyle<ImageStyle>',
    'false',
    'null',
  ]}
  webOptions={[]}
/>

### height

<ApiDetails
  componentName="RemoteImage"
  propertyName="height"
  required={false}
  defaultValue=""
  mobileOptions={['number', 'string']}
  webOptions={['number', 'string']}
/>

### opacity <Badge variant="mobile" />

Opacity of element

<ApiDetails
  componentName="RemoteImage"
  propertyName="opacity"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={[]}
/>

### resizeMode

<ApiDetails
  componentName="RemoteImage"
  propertyName="resizeMode"
  required={false}
  defaultValue=""
  mobileOptions={['center', 'contain', 'cover', 'repeat', 'stretch']}
  webOptions={['contain', 'cover']}
/>

### shape

<ApiDetails
  componentName="RemoteImage"
  propertyName="shape"
  required={false}
  defaultValue=""
  mobileOptions={['circle', 'rectangle', 'square', 'squircle']}
  webOptions={['circle', 'rectangle', 'square', 'squircle']}
/>

### shouldApplyDarkModeEnhacements <Badge variant="mobile" />

Fill in transparent background with inverted background color and add border. This solves issue of transparent, stamped out asset icons not being visible on dark backgrounds.

<ApiDetails
  componentName="RemoteImage"
  propertyName="shouldApplyDarkModeEnhacements"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

### source

<ApiDetails
  componentName="RemoteImage"
  propertyName="source"
  required={true}
  defaultValue=""
  mobileOptions={['ImageURISource', 'ImageURISource[]', 'number']}
  webOptions={[]}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="RemoteImage"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### width

<ApiDetails
  componentName="RemoteImage"
  propertyName="width"
  required={false}
  defaultValue=""
  mobileOptions={['number', 'string']}
  webOptions={['number', 'string']}
/>
