## A11y

By default Grid is not accessibility aware as it renders a standard `div`. We highly encourage the use of semantic elements so that screen readers can easily infer intent. This can be achieved through the `as` and `role` props.

```jsx
<Grid as="aside" columns={1} role="complementary">
  <SideBar />
</Grid>
```
