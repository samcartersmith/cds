import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/tabs/TabIndicator](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/tabs/TabIndicator.tsx)

<ComponentDetails componentName="TabIndicator" templateName="intro" />

## Usage

<ComponentDetails componentName="TabIndicator" templateName="usage" />

## Accessibility

<ComponentDetails componentName="TabIndicator" templateName="a11y" />

## Props

### background

This should always match the background color of the parent container

<ApiDetails
  componentName="TabIndicator"
  propertyName="background"
  required={false}
  defaultValue=": 'background'"
  mobileOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
  webOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
/>

<ComponentDetails componentName="TabIndicator" templateName="prop-background" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="TabIndicator"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="TabIndicator" templateName="prop-testID" />

### width

The width of the active TabLabel.

<ApiDetails
  componentName="TabIndicator"
  propertyName="width"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="TabIndicator" templateName="prop-width" />

### x

The xPosition of the active TabLabel.

<ApiDetails
  componentName="TabIndicator"
  propertyName="x"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="TabIndicator" templateName="prop-x" />

<ComponentDetails componentName="TabIndicator" templateName="outro" />
