## A11y

`Dropdown` comes with baked in accessibility attributes for both the subject and the content --the elevated surface that is toggled by interacting with the subject. To ensure the best user experience, we advise that you pass an `accessibilityLabel` to `Dropdown` to ensure that all `Dropdown`s have unique and indicative labels.
