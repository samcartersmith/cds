import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessibilityHint <Badge variant="web" />

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="Dropdown"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### accessibilityLabel <Badge variant="web" />

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="Dropdown"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="Dropdown"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### block <Badge variant="web" />

Makes the Popover Subject fill the width of the parent container

<ApiDetails
  componentName="Dropdown"
  propertyName="block"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### children <Badge variant="web" />

Subject of the Popover that when interacted with will toggle the visibility of the content

<ApiDetails
  componentName="Dropdown"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### content <Badge variant="web" />

<ApiDetails
  componentName="Dropdown"
  propertyName="content"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### contentPosition <Badge variant="web" />

Override content positioning defaults

<ApiDetails
  componentName="Dropdown"
  propertyName="contentPosition"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['PopoverContentPositionConfig']}
/>

### disableCloseOnOptionChange <Badge variant="web" />

Prevent menu from closing when an option is selected

<ApiDetails
  componentName="Dropdown"
  propertyName="disableCloseOnOptionChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### disablePortal <Badge variant="web" />

Does not render the Dropdown inside of a portal (react-dom createPortal). Portal is automatically disabled for SSR

<ApiDetails
  componentName="Dropdown"
  propertyName="disablePortal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### enableMobileModal <Badge variant="web" />

Display a Modal on mobile web instead of a relatively positioned Dropdown

<ApiDetails
  componentName="Dropdown"
  propertyName="enableMobileModal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### maxHeight <Badge variant="web" />

Can optionally pass a maxHeight.

<ApiDetails
  componentName="Dropdown"
  propertyName="maxHeight"
  required={false}
  defaultValue="300"
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

### maxWidth <Badge variant="web" />

Maximum width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="Dropdown"
  propertyName="maxWidth"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

### minWidth <Badge variant="web" />

Minimum width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="Dropdown"
  propertyName="minWidth"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

### onBlur <Badge variant="web" />

Callback that fires when Dropdown or trigger are blurred

<ApiDetails
  componentName="Dropdown"
  propertyName="onBlur"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['NoopFn']}
/>

### onChange <Badge variant="web" />

Callback that is fired whenever a select option is selected

<ApiDetails
  componentName="Dropdown"
  propertyName="onChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['(newValue: string) => void', 'SetState<string>']}
/>

### onCloseMenu <Badge variant="web" />

Callback that fires when Dropdown is closed

<ApiDetails
  componentName="Dropdown"
  propertyName="onCloseMenu"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['NoopFn']}
/>

### onOpenMenu <Badge variant="web" />

Callback that fires when Dropdown is opened

<ApiDetails
  componentName="Dropdown"
  propertyName="onOpenMenu"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['NoopFn']}
/>

### showOverlay <Badge variant="web" />

Display an overlay over all content below the Popover menu

<ApiDetails
  componentName="Dropdown"
  propertyName="showOverlay"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Dropdown"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### value <Badge variant="web" />

Default selected value, or preselected value

<ApiDetails
  componentName="Dropdown"
  propertyName="value"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

### width <Badge variant="web" />

Width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="Dropdown"
  propertyName="width"
  required={false}
  defaultValue="100%"
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>
