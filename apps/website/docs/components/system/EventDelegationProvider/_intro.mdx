Provides global event delegation configuration for the app. This should be used to attach event listeners to specific component actions.

If an event configuration is provided it will create event listeners to specific components and actions according to configuration items.

The event delegation configuration must be used in conjunction with the [EventCustomConfig](to="https://github.cbhq.net/frontend/cds/blob/master/packages/web/system/EventDelegationProvider.tsx")
