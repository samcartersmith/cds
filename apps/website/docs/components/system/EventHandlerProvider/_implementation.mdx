import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### config

<ApiDetails
  componentName="EventHandlerProvider"
  propertyName="config"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Partial<Record<Button, Partial<Record<EventHandlerAction, EventHandlerCallback>>>>',
  ]}
  webOptions={[
    'Partial<Record<Button, Partial<Record<EventHandlerAction, EventHandlerCallback>>>>',
  ]}
/>
