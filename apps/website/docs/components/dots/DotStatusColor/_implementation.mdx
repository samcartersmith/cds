import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### accessibilityHint

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="DotStatusColor"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="DotStatusColor"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### accessibilityLabelledBy

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="DotStatusColor"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### children

Children of where the dot will anchor to

<ApiDetails
  componentName="DotStatusColor"
  propertyName="children"
  required={false}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### overlap

Indicates what shape Dot is overlapping

<ApiDetails
  componentName="DotStatusColor"
  propertyName="overlap"
  required={false}
  defaultValue=""
  mobileOptions={['circular']}
  webOptions={['circular']}
/>

### pin

Position of dot relative to its parent

<ApiDetails
  componentName="DotStatusColor"
  propertyName="pin"
  required={false}
  defaultValue=""
  mobileOptions={['bottom-end', 'bottom-start', 'top-end', 'top-start']}
  webOptions={['bottom-end', 'bottom-start', 'top-end', 'top-start']}
/>

### size

Size of dot

<ApiDetails
  componentName="DotStatusColor"
  propertyName="size"
  required={false}
  defaultValue=""
  mobileOptions={['l', 'm', 's', 'xs']}
  webOptions={['l', 'm', 's', 'xs']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="DotStatusColor"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### variant

Background color of dot

<ApiDetails
  componentName="DotStatusColor"
  propertyName="variant"
  required={true}
  defaultValue=""
  mobileOptions={['foregroundMuted', 'negative', 'positive', 'primary']}
  webOptions={['foregroundMuted', 'negative', 'positive', 'primary']}
/>
