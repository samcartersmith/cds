import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/visualizations/SparklineInteractiveHeader](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/visualizations/SparklineInteractiveHeader.tsx)

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="intro" />

## Usage

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="usage" />

## Accessibility

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="a11y" />

## Props

### defaultLabel

Default label, changing this prop has no effect once the default is rendered.

<ApiDetails
  componentName="SparklineInteractiveHeader"
  propertyName="defaultLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="prop-defaultLabel" />

### defaultSubHead

Default SubHead, changing this prop has no effect once the default is rendered.

<ApiDetails
  componentName="SparklineInteractiveHeader"
  propertyName="defaultSubHead"
  required={false}
  defaultValue=""
  mobileOptions={['SparklineInteractiveSubHead']}
  webOptions={['SparklineInteractiveSubHead']}
/>

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="prop-defaultSubHead" />

### defaultTitle

Default title, changing this prop has no effect once the default is rendered.

<ApiDetails
  componentName="SparklineInteractiveHeader"
  propertyName="defaultTitle"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="prop-defaultTitle" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="SparklineInteractiveHeader"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="prop-testID" />

### trailing <Badge variant="mobile" />

Adds content next to the header. This is useful for interactive buttons

<ApiDetails
  componentName="SparklineInteractiveHeader"
  propertyName="trailing"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="prop-trailing" />

<ComponentDetails componentName="SparklineInteractiveHeader" templateName="outro" />
