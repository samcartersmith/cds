import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/visualizations/Sparkline](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/visualizations/Sparkline.tsx)

<ComponentDetails componentName="Sparkline" templateName="intro" />

## Usage

<ComponentDetails componentName="Sparkline" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Sparkline" templateName="a11y" />

## Props

### background <Badge variant="danger" order={1} />

Only use if you have a non CDS color powering the background color that a Sparkline will appear on top of. This is needed to validate the the `color` prop is an accessible color, otherwise it will pick an appropriate replacement. Accepts any valid color (hex, rgb, rgba).

<ApiDetails
  componentName="Sparkline"
  propertyName="background"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-background" />

### children

an optional SparklineArea that can be used to fill in the Sparkline

<ApiDetails
  componentName="Sparkline"
  propertyName="children"
  required={false}
  defaultValue=""
  mobileOptions={[
    'OptionalElement<SparklineAreaBaseProps>[]',
    'ReactElement<SparklineAreaBaseProps, string | JSXElementConstructor<any>>',
    'false',
    'null',
  ]}
  webOptions={[
    'OptionalElement<SparklineAreaBaseProps>[]',
    'ReactElement<SparklineAreaBaseProps, string | JSXElementConstructor<any>>',
    'false',
    'null',
  ]}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-children" />

### color

Color of the plotted Sparkline. Accepts any valid color value. Do not pass in a CDS alias such as foreground or a CSS Variable.

<ApiDetails
  componentName="Sparkline"
  propertyName="color"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-color" />

### height

Height of the Sparkline

<ApiDetails
  componentName="Sparkline"
  propertyName="height"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-height" />

### path

Svg path as string. CDS offers a `useSparklinePath` which is useful to generate this string. This is accessible via `import { useSparklinePath } from @cbhq/cds-common/visualizations/useSparklinePath;`. Alternatively, you can use product tailored tooling to generate the SVG path. This component only requires a valid path string is provided.

<ApiDetails
  componentName="Sparkline"
  propertyName="path"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-path" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Sparkline"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-testID" />

### width

Width of the Sparkline

<ApiDetails
  componentName="Sparkline"
  propertyName="width"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-width" />

### yAxisScalingFactor

Scales the sparkline to show more or less variance. Use a number less than 1 for less variance and a number greater than 1 for more variance. If you use a number greater than 1 it may clip the boundaries of the sparkline.

<ApiDetails
  componentName="Sparkline"
  propertyName="yAxisScalingFactor"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>

<ComponentDetails componentName="Sparkline" templateName="prop-yAxisScalingFactor" />

<ComponentDetails componentName="Sparkline" templateName="outro" />
