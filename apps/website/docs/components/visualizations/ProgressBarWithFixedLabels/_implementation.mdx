import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### disabled

Toggle used to show a disabled progress visualization

<ApiDetails
  componentName="ProgressBarWithFixedLabels"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### endLabel

Label that is pinned to the end of the container. If a number is used then it will format it as a percentage.

<ApiDetails
  componentName="ProgressBarWithFixedLabels"
  propertyName="endLabel"
  required={false}
  defaultValue=""
  mobileOptions={[
    'number',
    '{ value: number; render: (num: number, disabled?: boolean | undefined) => ReactNode; }',
  ]}
  webOptions={[
    'number',
    '{ value: number; render: (num: number, disabled?: boolean | undefined) => ReactNode; }',
  ]}
/>

### labelPlacement

Position of label relative to the bar

<ApiDetails
  componentName="ProgressBarWithFixedLabels"
  propertyName="labelPlacement"
  required={false}
  defaultValue="beside"
  mobileOptions={['above', 'below', 'beside']}
  webOptions={['above', 'below', 'beside']}
/>

### startLabel

Label that is pinned to the start of the container. If a number is used then it will format it as a percentage.

<ApiDetails
  componentName="ProgressBarWithFixedLabels"
  propertyName="startLabel"
  required={false}
  defaultValue=""
  mobileOptions={[
    'number',
    '{ value: number; render: (num: number, disabled?: boolean | undefined) => ReactNode; }',
  ]}
  webOptions={[
    'number',
    '{ value: number; render: (num: number, disabled?: boolean | undefined) => ReactNode; }',
  ]}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="ProgressBarWithFixedLabels"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>
