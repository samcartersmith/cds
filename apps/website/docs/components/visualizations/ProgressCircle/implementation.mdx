import { ApiDetails } from ":cds-website/components/ApiDetails";
import { Badge } from ":cds-website/components/Badge";
import { ComponentDetails } from ":cds-website/components/ComponentDetails";
import { RightSidebar } from ":cds-website/src/theme/RightSidebar";

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/visualizations/ProgressCircle](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/visualizations/ProgressCircle.tsx)

<ComponentDetails componentName="ProgressCircle" templateName="intro" />

## Usage

<ComponentDetails componentName="ProgressCircle" templateName="usage" />

## Accessibility

<ComponentDetails componentName="ProgressCircle" templateName="a11y" />

## Props

### color

Custom progress color.

<ApiDetails
  componentName="ProgressCircle"
  propertyName="color"
  required={false}
  defaultValue="primary"
  mobileOptions={["foreground", "negative", "positive", "primary"]}
  webOptions={["foreground", "negative", "positive", "primary"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-color" />

### disabled

Toggle used to show a disabled progress visualization

<ApiDetails
  componentName="ProgressCircle"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={["false", "true"]}
  webOptions={["false", "true"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-disabled" />

### hideText

Toggle used to hide the inner circle percentage

<ApiDetails
  componentName="ProgressCircle"
  propertyName="hideText"
  required={false}
  defaultValue=""
  mobileOptions={["false", "true"]}
  webOptions={["false", "true"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-hideText" />

### progress

Number between 0-1 representing the progress percentage

<ApiDetails
  componentName="ProgressCircle"
  propertyName="progress"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-progress" />

### size

Optional size in px for the visualization. This is useful if the visualization is used in an HStack. If this is omitted the visualization will fill the parent width or height. Since its a circular visualization it will fill the smaller of the parent width or height.

<ApiDetails
  componentName="ProgressCircle"
  propertyName="size"
  required={false}
  defaultValue=""
  mobileOptions={["number"]}
  webOptions={["number"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-size" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="ProgressCircle"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={["string"]}
  webOptions={["string"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-testID" />

### weight

Toggle used to show a thicker progress visualization

<ApiDetails
  componentName="ProgressCircle"
  propertyName="weight"
  required={false}
  defaultValue="normal"
  mobileOptions={["heavy", "normal"]}
  webOptions={["heavy", "normal"]}
/>

<ComponentDetails componentName="ProgressCircle" templateName="prop-weight" />

<ComponentDetails componentName="ProgressCircle" templateName="outro" />
