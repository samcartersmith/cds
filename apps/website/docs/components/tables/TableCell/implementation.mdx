import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/tables/TableCell](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/tables/TableCell.tsx)

<ComponentDetails componentName="TableCell" templateName="intro" />

## Usage

<ComponentDetails componentName="TableCell" templateName="usage" />

## Accessibility

<ComponentDetails componentName="TableCell" templateName="a11y" />

## Props

### alignItems <Badge variant="web" />

This prop us useful for aligning the last item to the right, or top-aligning cells

<ApiDetails
  componentName="TableCell"
  propertyName="alignItems"
  required={false}
  defaultValue="'center'"
  mobileOptions={[]}
  webOptions={['center', 'flex-end', 'flex-start']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-alignItems" />

### children <Badge variant="web" />

Children to render within the cell. If children are provided, title, subtitle and description MUST be unset

<ApiDetails
  componentName="TableCell"
  propertyName="children"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[
    'Iterable<ReactNode>',
    'Iterable<ReactNode> & ReactElement<any, string | JSXElementConstructor<any>>',
    'Iterable<ReactNode> & ReactPortal',
    'Iterable<ReactNode> & false',
    'Iterable<ReactNode> & number',
    'Iterable<ReactNode> & string',
    'Iterable<ReactNode> & true',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactElement<any, string | JSXElementConstructor<any>> & Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>> & ReactPortal',
    'ReactElement<any, string | JSXElementConstructor<any>> & false',
    'ReactElement<any, string | JSXElementConstructor<any>> & number',
    'ReactElement<any, string | JSXElementConstructor<any>> & string',
    'ReactElement<any, string | JSXElementConstructor<any>> & true',
    'ReactNode[]',
    'ReactNode[] & Iterable<ReactNode>',
    'ReactNode[] & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNode[] & ReactPortal',
    'ReactNode[] & false',
    'ReactNode[] & number',
    'ReactNode[] & string',
    'ReactNode[] & true',
    'ReactPortal',
    'ReactPortal & Iterable<ReactNode>',
    'ReactPortal & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal & false',
    'ReactPortal & number',
    'ReactPortal & string',
    'ReactPortal & true',
    'false',
    'false & Iterable<ReactNode>',
    'false & ReactElement<any, string | JSXElementConstructor<any>>',
    'false & ReactPortal',
    'false & {}',
    'number',
    'number & Iterable<ReactNode>',
    'number & ReactElement<any, string | JSXElementConstructor<any>>',
    'number & ReactPortal',
    'number & {}',
    'string',
    'string & Iterable<ReactNode>',
    'string & ReactElement<any, string | JSXElementConstructor<any>>',
    'string & ReactPortal',
    'string & {}',
    'true',
    'true & Iterable<ReactNode>',
    'true & ReactElement<any, string | JSXElementConstructor<any>>',
    'true & ReactPortal',
    'true & {}',
    '{}',
    '{} & false',
    '{} & number',
    '{} & string',
    '{} & true',
  ]}
/>

<ComponentDetails componentName="TableCell" templateName="prop-children" />

### color <Badge variant="web" />

The color for all text components rendered inside the TableCell. Use titleColor and subtitleColor if you need to be more specific

<ApiDetails
  componentName="TableCell"
  propertyName="color"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
/>

<ComponentDetails componentName="TableCell" templateName="prop-color" />

### colSpan <Badge variant="web" />

The colSpan attribute defines the number of columns a cell should span

<ApiDetails
  componentName="TableCell"
  propertyName="colSpan"
  required={false}
  defaultValue="1"
  mobileOptions={[]}
  webOptions={['number']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-colSpan" />

### dangerouslySetHtmlWidth <Badge variant="web" /> <Badge variant="danger" order={2} /> <Badge variant="deprecated" order={3} />

is deprecated, and should _only_ be used to unblock migration efforts
HTML width attribute to help with column layout.This attr

<ApiDetails
  componentName="TableCell"
  propertyName="dangerouslySetHtmlWidth"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['number', 'string']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-dangerouslySetHtmlWidth" />

### direction <Badge variant="web" />

Direction provides content flow control. Use vertical to inherit a VStask, horizontal for an HStack

<ApiDetails
  componentName="TableCell"
  propertyName="direction"
  required={false}
  defaultValue="vertical"
  mobileOptions={[]}
  webOptions={['horizontal', 'vertical']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-direction" />

### end <Badge variant="web" />

Element (icon, asset, image, etc) to display at the end of the cell

<ApiDetails
  componentName="TableCell"
  propertyName="end"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-end" />

### innerSpacing <Badge variant="web" />

The spacing to use on the inner content of Cell

<ApiDetails
  componentName="TableCell"
  propertyName="innerSpacing"
  required={false}
  defaultValue="{ spacingHorizontal: 0 }"
  mobileOptions={[]}
  webOptions={['CellSpacing']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-innerSpacing" />

### justifyContent <Badge variant="web" />

This prop us useful for right-aligning the last column

<ApiDetails
  componentName="TableCell"
  propertyName="justifyContent"
  required={false}
  defaultValue="'flex-start'"
  mobileOptions={[]}
  webOptions={['center', 'flex-end', 'flex-start', 'space-around', 'space-between', 'space-evenly']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-justifyContent" />

### onPress <Badge variant="web" />

Callback to fire when pressed

<ApiDetails
  componentName="TableCell"
  propertyName="onPress"
  required={false}
  defaultValue="noop"
  mobileOptions={[]}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-onPress" />

### outerSpacing <Badge variant="web" />

The spacing to use on the parent wrapper of Cell

<ApiDetails
  componentName="TableCell"
  propertyName="outerSpacing"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['CellSpacing']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-outerSpacing" />

### overflow <Badge variant="web" />

Should the title/subtitle text truncate

<ApiDetails
  componentName="TableCell"
  propertyName="overflow"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['clip', 'truncate', 'wrap']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-overflow" />

### start <Badge variant="web" />

Element (icon, asset, image, etc) to display at the start of the cell

<ApiDetails
  componentName="TableCell"
  propertyName="start"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-start" />

### subtitle <Badge variant="web" />

A subtitle will appear below the title. Type is set with TextLabel2

<ApiDetails
  componentName="TableCell"
  propertyName="subtitle"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-subtitle" />

### subtitleColor <Badge variant="web" />

<ApiDetails
  componentName="TableCell"
  propertyName="subtitleColor"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
/>

<ComponentDetails componentName="TableCell" templateName="prop-subtitleColor" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="TableCell"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-testID" />

### title <Badge variant="web" />

If a title is provided, the default type style will be inferred from context. If the cell is rendered in a TableHeader, it will render as a TextHeadline. But when rendered in a TableBody the text will render as TextBody.

<ApiDetails
  componentName="TableCell"
  propertyName="title"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-title" />

### titleColor <Badge variant="web" />

<ApiDetails
  componentName="TableCell"
  propertyName="titleColor"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
/>

<ComponentDetails componentName="TableCell" templateName="prop-titleColor" />

### width <Badge variant="web" />

As a convenience, the width prop will set the css width and maxWidth props

<ApiDetails
  componentName="TableCell"
  propertyName="width"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['number', 'string']}
/>

<ComponentDetails componentName="TableCell" templateName="prop-width" />

<ComponentDetails componentName="TableCell" templateName="outro" />
