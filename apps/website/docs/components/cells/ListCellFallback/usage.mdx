Supports the same structure and similar props as `ListCell`, but instead of accepting content, it accepts a boolean. This boolean determines whether to show a fallback shimmer in the desired location.

```jsx live
<ListCellFallback title description detail media="avatar" />
```
