import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### compact

enables compact spacing

<ApiDetails
  componentName="ListCellFallback"
  propertyName="compact"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### description

Display description shimmer.

<ApiDetails
  componentName="ListCellFallback"
  propertyName="description"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### detail

Display detail shimmer.

<ApiDetails
  componentName="ListCellFallback"
  propertyName="detail"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### media

Display media shimmer with a shape according to type.

<ApiDetails
  componentName="ListCellFallback"
  propertyName="media"
  required={false}
  defaultValue=""
  mobileOptions={['asset', 'avatar', 'icon', 'image', 'pictogram']}
  webOptions={['asset', 'avatar', 'icon', 'image', 'pictogram']}
/>

### subdetail

Display subdetail shimmer.

<ApiDetails
  componentName="ListCellFallback"
  propertyName="subdetail"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### title

Display title shimmer.

<ApiDetails
  componentName="ListCellFallback"
  propertyName="title"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>
