## Responsive Styles

**Web only** You can create responsive layouts by passing a configuration object to a ListCell and specify unique styles for phones, tablets, or desktops.

Please note, there are no default responsive styles for ListCell. This is an additive feature that you will need to configure yourself.

### Supported Properties

- innerPadding
- outerPadding

### Usage

Always hoist or memoize your responsive style configurations.

```jsx live
function ResponsiveListCell() {
  const responsiveConfig = useMemo(
    () => ({
      phone: {
        innerPadding: {
          spacingHorizontal: 2,
        },
        outerPadding: {
          spacingVertical: 1,
        },
      },
      desktop: {
        innerPadding: {
          spacingHorizontal: 4,
        },
        outerPadding: {
          spacingVertical: 2,
        },
      },
    }),
    [],
  );

  return (
    <ListCell
      responsiveConfig={responsiveConfig}
      onPress={() => console.log('Pressed')}
      title="Title"
      description="Description"
    />
  );
}
```
