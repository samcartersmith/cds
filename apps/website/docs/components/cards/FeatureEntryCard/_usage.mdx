```jsx live
function Example() {
  const cardProps = {
    title: 'Send a crypto gift',
    description: 'Share the gift of crypto this holiday season',
    spotSquare: 'coinbaseCardSparkle',
    actionLabel: 'Start gifting',
    onActionPress: () => console.log('pressed'),
  };
  return (
    <ResponsiveExample>
      <CardGroup>
        <FeatureEntryCard {...cardProps} />
        <FeatureEntryCard {...cardProps} />
        <FeatureEntryCard {...cardProps} />
        <FeatureEntryCard {...cardProps} />
      </CardGroup>
    </ResponsiveExample>
  );
}
```
