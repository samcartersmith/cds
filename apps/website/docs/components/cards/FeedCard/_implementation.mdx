import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### avatarUrl

Absolute or Relative to Avatar

<ApiDetails
  componentName="FeedCard"
  propertyName="avatarUrl"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### bodyDescription

Text to be displayed in TextBody

<ApiDetails
  componentName="FeedCard"
  propertyName="bodyDescription"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### bodyMediaUrl

Absolute or Relative to Image

<ApiDetails
  componentName="FeedCard"
  propertyName="bodyMediaUrl"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### bodyOrientation

Vertical places media above text content, Horizontal places media to the side of text content

<ApiDetails
  componentName="FeedCard"
  propertyName="bodyOrientation"
  required={true}
  defaultValue=""
  mobileOptions={['horizontal', 'vertical']}
  webOptions={['horizontal', 'vertical']}
/>

### bodyTitle

Text to be displayed in TextHeadline

<ApiDetails
  componentName="FeedCard"
  propertyName="bodyTitle"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### footerActions

ReactNode that should be one or more actions

<ApiDetails
  componentName="FeedCard"
  propertyName="footerActions"
  required={true}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### headerActionNode

IconButton ReactNode

<ApiDetails
  componentName="FeedCard"
  propertyName="headerActionNode"
  required={true}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### headerDescription

Text to be displayed in TextCaption

<ApiDetails
  componentName="FeedCard"
  propertyName="headerDescription"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### headerMetaData

Meta Data Text to be displayed in TextLegal

<ApiDetails
  componentName="FeedCard"
  propertyName="headerMetaData"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="FeedCard"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>
