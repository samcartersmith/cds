import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';

## Props

### children

Collapsible content

<ApiDetails
  componentName="AccordionItem"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'Iterable<ReactNode>',
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

### headerRef

<ApiDetails
  componentName="AccordionItem"
  propertyName="headerRef"
  required={false}
  defaultValue=""
  mobileOptions={['RefObject<View>']}
  webOptions={['RefObject<HTMLButtonElement>']}
/>

### itemKey

Key of the accordion item. This should be unique inside the same Accordion unless you want multiple items to be controlled at the same time.

<ApiDetails
  componentName="AccordionItem"
  propertyName="itemKey"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### media

<ApiDetails
  componentName="AccordionItem"
  propertyName="media"
  required={false}
  defaultValue=""
  mobileOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
  webOptions={['ReactElement<any, string | JSXElementConstructor<any>>']}
/>

### onPress

Callback function fired when the accordion item is pressed

<ApiDetails
  componentName="AccordionItem"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['(key: string) => void']}
  webOptions={['(key: string) => void']}
/>

### panelRef

<ApiDetails
  componentName="AccordionItem"
  propertyName="panelRef"
  required={false}
  defaultValue=""
  mobileOptions={['RefObject<View>']}
  webOptions={['RefObject<HTMLDivElement>']}
/>

### subtitle

Subtitle of the accordion item

<ApiDetails
  componentName="AccordionItem"
  propertyName="subtitle"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="AccordionItem"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### title

Title of the accordion item

<ApiDetails
  componentName="AccordionItem"
  propertyName="title"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>
