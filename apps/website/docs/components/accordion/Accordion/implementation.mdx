import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/accordion/Accordion](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/accordion/Accordion.tsx)

<ComponentDetails componentName="Accordion" templateName="intro" />

## Usage

<ComponentDetails componentName="Accordion" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Accordion" templateName="a11y" />

## Props

### defaultActiveKey

Default active accordion item key. If not specified or does not exist in the accordion items, all items will be closed on mount

<ApiDetails
  componentName="Accordion"
  propertyName="defaultActiveKey"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Accordion" templateName="prop-defaultActiveKey" />

### onItemPress

Callback function fired when any of accordion items is pressed

<ApiDetails
  componentName="Accordion"
  propertyName="onItemPress"
  required={false}
  defaultValue=""
  mobileOptions={['(key: string) => void']}
  webOptions={['(key: string) => void']}
/>

<ComponentDetails componentName="Accordion" templateName="prop-onItemPress" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Accordion"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Accordion" templateName="prop-testID" />

<ComponentDetails componentName="Accordion" templateName="outro" />
