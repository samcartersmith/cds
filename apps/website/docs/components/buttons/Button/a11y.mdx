If the button label isn't descriptive enough, an additional text label must be provided to identify the control for accessibility using the `accessibilityLabel` prop.

```jsx
<Button accessibilityLabel="Update profile settings">Update</Button>
```
