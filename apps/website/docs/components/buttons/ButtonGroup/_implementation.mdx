import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

### accessibilityLabel

Accessibility label describing the group of buttons.

<ApiDetails
  componentName="ButtonGroup"
  propertyName="accessibilityLabel"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

### block

Expand buttons to fill available space within the group.

<ApiDetails
  componentName="ButtonGroup"
  propertyName="block"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

### children

Buttons to render as a group.

<ApiDetails
  componentName="ButtonGroup"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[
    'OptionalElement<ButtonBaseProps>[]',
    'ReactElement<ButtonBaseProps, string | JSXElementConstructor<any>>',
    'false',
    'null',
  ]}
  webOptions={[
    'OptionalElement<ButtonBaseProps>[]',
    'ReactElement<ButtonBaseProps, string | JSXElementConstructor<any>>',
    'false',
    'null',
  ]}
/>

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="ButtonGroup"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

### vertical

Stack buttons vertically instead of horizontally.

<ApiDetails
  componentName="ButtonGroup"
  propertyName="vertical"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>
