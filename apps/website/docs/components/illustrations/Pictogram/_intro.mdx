import { IllustrationSheet } from '../IllustrationSheet';
import { IllustrationAnnouncement } from '../IllustrationAnnouncement';

Pictograms are elevated product icons. They should be literal, concise, and used for glanceability purposes but the intention here is to grab the attention of users. We now support
searching for illustration using keywords. For instance, if you search "bank", "addPayment", "fiat", "creditCard" will show up as part of the result. As the designers believe that these illustrations represents "bank".

<IllustrationAnnouncement />

<IllustrationSheet variant="pictogram" />
