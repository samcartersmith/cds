import { IllustrationSheet } from '../IllustrationSheet';
import { IllustrationAnnouncement } from '../IllustrationAnnouncement';

A SpotSquare tells a singular concept and carries the most literal expression. We now support searching for illustration using keywords. For instance, if you search "card", "gifting" will show up as part of the result. As the designers believe "gifting" and "card" are semantically correlated.

<IllustrationAnnouncement />

<IllustrationSheet variant="spotSquare" />
