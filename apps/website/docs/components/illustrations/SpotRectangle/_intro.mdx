import { IllustrationSheet } from '../IllustrationSheet';
import { IllustrationAnnouncement } from '../IllustrationAnnouncement';

<IllustrationAnnouncement />

Telling a singular concept and carry the most literal expression. SpotRectangle Size (240px x 120px). We now support
searching for illustration using keywords. For instance, if you search "money", "highFees", "trade", "transferCoins" will show up as part of the result. As the designers believe that these illustrations represents "money".

<IllustrationSheet variant="spotRectangle" />
