import Npmrc from './_npmrc.mdx';
import Providers, { toc as providersToc } from './_providers-mobile.mdx';
import Eslint, { toc as eslintToc } from './_eslint-mobile.mdx';
import mobilePackage from ':docgen/mobile/metadata';
import illustrationPackage from ':docgen/illustrations/metadata';
import iconPackage from ':docgen/icons/metadata';
import CodeBlock from '@theme/CodeBlock';

export const tocOverride = [...toc, ...providersToc, ...eslintToc];

## React Native

### Setup

<Npmrc />

### Install

<CodeBlock language="bash">{`yarn add @cbhq/cds-mobile@^${mobilePackage.version} @cbhq/cds-icons@^${iconPackage.version} @cbhq/cds-illustrations@^${illustrationPackage.version}`}</CodeBlock>

### Add providers

<Providers />

### ESLint

<Eslint />
