import Npmrc from './_npmrc.mdx';
import Providers, { toc as providersToc } from './_providers-mobile.mdx';
import metadata from ':docgen/mobile/metadata';
import CodeBlock from '@theme/CodeBlock';

export const tocOverride = [...toc, ...providersToc];

## React Native

### Setup

<Npmrc />

### Install

<CodeBlock language="bash">{`yarn add @cbhq/cds-mobile@^${metadata.version}`}</CodeBlock>

### Add providers

<Providers />
