import Npmrc from './_npmrc.mdx';
import Providers, { toc as providersToc } from './_providers-web.mdx';
import Eslint, { toc as eslintToc } from './_eslint-web.mdx';
import Fout, { toc as foutToc } from './_fout.mdx';
import webPackage from ':docgen/web/metadata';
import illustrationPackage from ':docgen/illustrations/metadata';
import iconPackage from ':docgen/icons/metadata';
import CodeBlock from '@theme/CodeBlock';

export const tocOverride = [...toc, ...providersToc, ...eslintToc];

## Web

If your web environment has a css-loader for node_modules.

### Setup

<Npmrc />

### Install

<CodeBlock language="bash">{`yarn add @cbhq/cds-web@^${webPackage.version} @cbhq/cds-icons@^${iconPackage.version} @cbhq/cds-illustrations@^${illustrationPackage.version}`}</CodeBlock>

### Add root imports

```tsx
// This includes font face declarations and focus-visible polyfill & has side-effects.
// This should go at top of app entry before CDS components.
import '@cbhq/cds-web/globalStyles';
```

### Add providers

<Providers />

### ESLint

<Eslint />

### Flash of Unstyled Text (FOUT)

<Fout />
