### Simple Page Header

```jsx
function Example() {
  return (
    <PageHeader
      background="bgPrimaryWash"
      end={
        <HStack alignItems="center" gap={2}>
          <Link to="https://cds.cbhq.net/components/link">
            <TextHeadline as="p" color="fgPrimary">
              Help
            </TextHeadline>
          </Link>
          <HStack gap={1}>
            <IconButton
              transparent
              accessibilityLabel="Share"
              name="externalLink"
              onClick={() => console.log()}
              testID="header-external-link-button"
            />
            <IconButton
              transparent
              accessibilityLabel="Close"
              name="close"
              onClick={() => console.log()}
              testID="header-close-button"
            />
          </HStack>
        </HStack>
      }
      start={<LogoMark size={32} />}
      title={'Page Title'}
    />
  );
}
```
