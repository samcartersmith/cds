{
  "name": "{{NEW_PACKAGE}}",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/{{NEW_PACKAGE}}/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "command": "rm -rf esm lib && rollup -c",
      "options": {
        "cwd": "packages/{{NEW_PACKAGE}}"
      }
    },
    "typecheck": {
      "command": "tsc --build --pretty --verbose"
    }
  }
}
