{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "destinationDir": {
      "type": "string",
      "description": "relative path to output directory"
    },
    "staticFilesToIgnore": {
      "type": "array",
      "description": "Static files to remove from the output"
    },
    "babelConfig": {
      "type": "string",
      "description": "relative path to babel config file"
    },
    "babelExtensions": {
      "type": "array",
      "description": "array of extensions of files to compile with babel"
    },
    "babelIgnore": {
      "type": "array",
      "description": "array of globs of files to ignore"
    },
    "typescriptConfig": {
      "type": "string",
      "description": "relative path of the typescript config file"
    }
  }
}