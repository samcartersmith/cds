import { css } from 'linaria';

<% Object.entries(data).map(([name, styles]) => { %>
  export const <%- name %> = css`
    <%_ Object.entries(data[name]).map(([varName, value]) => { _%>
      <%- varName %>:  <%- value %>;
    <%_ }) _%>
  `;
<% }) %> 
