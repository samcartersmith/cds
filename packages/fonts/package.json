{
  "name": "@cbhq/cds-fonts",
  "version": "1.0.0",
  "description": "Coinbase Design System - Fonts",
  "keywords": [
    "cds",
    "design system",
    "fonts"
  ],
  "repository": "git@github.cbhq.net:frontend/cds.git",
  "authors": "Coinbase",
  "main": "./index",
  "devDependencies": {
    "@cbhq/cds-tools": "workspace:*"
  },
  "scripts": {
    "build": "yarn nx run fonts:build",
    "lint": "yarn nx run fonts:lint",
    "lint.fix": "yarn nx run fonts:lint --fix",
    "stylelint": "yarn nx run fonts:lint-styles",
    "stylelint.fix": "yarn nx run fonts:lint-styles --fix",
    "typecheck": "yarn nx run fonts:typecheck"
  }
}
