# @cbhq/cds-icons

CDS icons used in @cbhq/cds-web and @cbhq/cds-mobile.

## Sync icons from figma

To sync icons with Figma, run the command below and commit the generated files.

```shell
yarn nx run icons:sync
```

## Installation

```shell
yarn add @cbhq/cds-icons
```
