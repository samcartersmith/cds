{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "additionalProperties": false,
  "required": ["figmaApiFileId", "manifestFile", "generatedDirectory"],
  "properties": {
    "figmaApiFileId": {
      "type": "string",
      "description": "The file ID for the Figma file to pull styles from"
    },
    "changelogFile": {
      "type": "string",
      "description": "The CHANGELOG.md file to read and write overview of changes to"
    },
    "manifestFile": {
      "type": "string",
      "description": "The manifest.json file to track illustration syncs."
    },
    "generatedDirectory": {
      "type": "string",
      "description": "The directory to output generated files."
    },
    "generatedCssVariablesPrefix": {
      "type": "string",
      "description": "Prefix to add to generated css variables"
    }
  }
}
