# @cbhq/eslint-plugin-cds

## Development

To build locally, run 
```
yarn nx run eslint-plugin-cds:build   
```

Use [AST Explorer](https://astexplorer.net/) with parser set to `@typescript-eslint/parser` to determine AST node types.

