import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { withStoryLabel } from '@cbhq/cds-web/storybook-decorators/withStoryLabel';

import { ComposedSystem } from './NavigationStorySetup';

<Meta title="Overlays/Navigation/System" />

# Composed System

<Canvas isColumn withToolbar>
  <Story
    name="A full system example"
    decorators={[withStoryLabel('A full system example')]}
    parameters={{
      a11y: {
        config: {
          rules: [{ id: 'aria-required-children', enabled: false }],
        },
      },
    }}
  >
    <ComposedSystem />
  </Story>
</Canvas>
