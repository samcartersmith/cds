import { withStoryLabel } from '../../storybook-decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';

import { ThemeProvider } from '../../system/ThemeProvider';
import { FeatureFlagProvider } from '../../system/FeatureFlagProvider';
import { VStack } from '../../layout/VStack';
import {
  TextDisplay1,
  TextDisplay2,
  TextDisplay3,
  TextTitle1,
  TextTitle2,
  TextTitle3,
  TextTitle4,
  TextHeadline,
  TextBody,
  TextLabel1,
  TextLabel2,
  TextCaption,
  TextLegal,
} from '../index';

export const textComponents = [
  TextDisplay1,
  TextDisplay2,
  TextDisplay3,
  TextTitle1,
  TextTitle2,
  TextTitle3,
  TextTitle4,
  TextHeadline,
  TextBody,
  TextLabel1,
  TextLabel2,
  TextCaption,
  TextLegal,
];

<Meta
  title="Core Components/Text"
  component={TextDisplay1}
  argTypes={{
    children: { control: 'text' },
  }}
/>

# Text

<Canvas withToolbar>
  <Story
    name="Normal"
    decorators={[withStoryLabel('Normal (Large)')]}
    args={{ as: 'b', display: 'block' }}
  >
    {(args) => (
      <>
        {textComponents.map((Component) => {
          const name = Component.displayName.slice(4);
          return (
            <Component key={name} {...args}>
              {args.children || name}
            </Component>
          );
        })}
      </>
    )}
  </Story>
  <Story
    name="Dense"
    decorators={[withStoryLabel('Dense (xSmall)')]}
    args={{ as: 'p', display: 'block' }}
  >
    {(args) => (
      <ThemeProvider scale="xSmall">
        {textComponents.map((Component) => {
          const name = Component.displayName.slice(4);
          return (
            <Component key={name} {...args}>
              {args.children || name}
            </Component>
          );
        })}
      </ThemeProvider>
    )}
  </Story>
</Canvas>

# Frontier

<Canvas>
  <Story
    name="Frontier Normal"
    decorators={[withStoryLabel('Frontier Normal (Large)')]}
    args={{ as: 'b', display: 'block' }}
  >
    {(args) => (
      <FeatureFlagProvider frontierTypography>
        {textComponents.map((Component) => {
          const name = Component.displayName.slice(4);
          return (
            <Component key={name} {...args}>
              {args.children || name}
            </Component>
          );
        })}
      </FeatureFlagProvider>
    )}
  </Story>
  <Story
    name="Frontier Dense"
    decorators={[withStoryLabel('Frontier Dense (xSmall)')]}
    args={{ as: 'p', display: 'block' }}
  >
    {(args) => (
      <FeatureFlagProvider frontierTypography>
        <ThemeProvider scale="xSmall">
          {textComponents.map((Component) => {
            const name = Component.displayName.slice(4);
            return (
              <Component key={name} {...args}>
                {args.children || name}
              </Component>
            );
          })}
        </ThemeProvider>
      </FeatureFlagProvider>
    )}
  </Story>
</Canvas>

## Mono font

<Canvas>
  <Story name="Mono font" decorators={[withStoryLabel('Mono font')]} args={{ as: 'p', mono: true }}>
    {(args) => (
      <>
        {textComponents.map((Component) => {
          const name = Component.displayName.slice(4);
          return (
            <Component key={name} {...args}>
              {args.children || name}
            </Component>
          );
        })}
      </>
    )}
  </Story>
</Canvas>

## Numeric Styles

<Canvas>
  <Story
    name="Slashed zero"
    decorators={[withStoryLabel('Slashed Zero')]}
    args={{ as: 'p', slashedZero: true }}
  >
    {(args) => <TextBody {...args}>{args.children || 'OZY28019'}</TextBody>}
  </Story>
  <Story
    name="Tabular numbers"
    decorators={[withStoryLabel('Tabular numbers (align end)')]}
    args={{ as: 'p', tabularNumbers: true, align: 'end' }}
  >
    {(args) => (
      <>
        <TextBody {...args}>{args.children || '91.23450'}</TextBody>
        <TextBody {...args}>{args.children || '11.98762'}</TextBody>
      </>
    )}
  </Story>
</Canvas>

## Selectable

<Canvas>
  <Story
    name="Selectable none"
    decorators={[withStoryLabel('None')]}
    args={{ as: 'p', selectable: 'none' }}
  >
    {(args) => <TextBody {...args}>{args.children || 'BTC'}</TextBody>}
  </Story>
  <Story
    name="Selectable text"
    decorators={[withStoryLabel('Text')]}
    args={{ as: 'p', selectable: 'text' }}
  >
    {(args) => <TextBody {...args}>{args.children || 'Balance: 1,820,29.56'}</TextBody>}
  </Story>
  <Story
    name="Selectable all"
    decorators={[withStoryLabel('All')]}
    args={{ as: 'p', selectable: 'all', slashedZero: true }}
  >
    {(args) => (
      <TextBody {...args}>{args.children || 'bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh'}</TextBody>
    )}
  </Story>
</Canvas>

## Text Decoration

<Canvas>
  <Story
    name="Underline"
    decorators={[withStoryLabel('Underline')]}
    args={{ as: 'p', underline: true }}
  >
    {(args) => <TextBody {...args}>{args.children || 'Learn more'}</TextBody>}
  </Story>
  <Story name="Strikethrough" decorators={[withStoryLabel('Text')]} args={{ as: 's' }}>
    {(args) => <TextBody {...args}>{args.children || 'Manual verification'}</TextBody>}
  </Story>
</Canvas>

## No Wrap and Overflow

export const MediumContainer = (props) => (
  <div
    style={{ width: '35%', height: '50px', backgroundColor: 'var(--background-alternate)' }}
    {...props}
  />
);

<Canvas>
  <Story name="No wrap" decorators={[withStoryLabel('No wrap')]} args={{ as: 'p', noWrap: true }}>
    {(args) => (
      <MediumContainer>
        <TextBody {...args}>
          {args.children || 'As with any asset, the value of Digital Currencies can'}
        </TextBody>
      </MediumContainer>
    )}
  </Story>
  <Story
    name="Text overflow ellipsis"
    decorators={[withStoryLabel('Overflow ellipsis')]}
    args={{ as: 'p', overflow: 'truncate' }}
  >
    {(args) => (
      <MediumContainer>
        <TextBody {...args}>
          {args.children ||
            'As with any asset, the value of Digital Currencies can go up or down and there can be a substantial risk that you lose money buying, selling, holding, or investing in digital currencies. You should carefully consider whether trading or holding Digital Currencies is suitable for you in light of your financial condition. Coinbase is not registered with the U.S. Securities and Exchange Commission and does not offer securities services in the United States or to U.S. persons.'}
        </TextBody>
      </MediumContainer>
    )}
  </Story>
  <Story
    name="Text overflow clip"
    decorators={[withStoryLabel('Overflow clip')]}
    args={{ as: 'p', overflow: 'clip' }}
  >
    {(args) => (
      <MediumContainer>
        <TextBody {...args}>
          {args.children ||
            'Crypto address 0xf847047c69726b4049a5b866c8fa37cfe4ed614f. As with any asset, the value of Digital Currencies can go up or down and there can be a substantial risk that you lose money buying, selling, holding, or investing in digital currencies. You should carefully consider whether trading or holding Digital Currencies is suitable for you in light of your financial condition. Coinbase is not registered with the U.S. Securities and Exchange Commission and does not offer securities services in the United States or to U.S. persons.'}
        </TextBody>
      </MediumContainer>
    )}
  </Story>
  <Story
    name="Text numberOfLines"
    decorators={[withStoryLabel('numberOfLines')]}
    args={{
      as: 'p',
      numberOfLines: 2,
      children:
        'Crypto address 0xf847047c69726b4049a5b866c8fa37cfe4ed614f. As with any asset, the value of Digital Currencies can go up or down and there can be a substantial risk that you lose money buying, selling, holding, or investing in digital currencies. You should carefully consider whether trading or holding Digital Currencies is suitable for you in light of your financial condition. Coinbase is not registered with the U.S. Securities and Exchange Commission and does not offer securities services in the United States or to U.S. persons.',
    }}
  >
    {(args) => (
      <VStack maxWidth={300}>
        <TextBody {...args} />
      </VStack>
    )}
  </Story>
  <Story
    name="Text numberOfLines siblings"
    decorators={[withStoryLabel('numberOfLines')]}
    args={{
      as: 'p',
      numberOfLines: 2,
      overflow: 'break',
      children:
        'Crypto address 0xf847047c69726b4049a5b866c8fa37cfe4ed614f. As with any asset, the value of Digital Currencies can go up or down and there can be a substantial risk that you lose money buying, selling, holding, or investing in digital currencies. You should carefully consider whether trading or holding Digital Currencies is suitable for you in light of your financial condition. Coinbase is not registered with the U.S. Securities and Exchange Commission and does not offer securities services in the United States or to U.S. persons.',
    }}
  >
    {(args) => (
      <VStack maxWidth={300} gap={3}>
        <TextBody as="p" {...args} numberOfLines={args.numberOfLines - 1} />
        <TextBody as="p" {...args} numberOfLines={args.numberOfLines} />
        <TextBody as="p" {...args} numberOfLines={args.numberOfLines + 1} />
      </VStack>
    )}
  </Story>
  <Story
    name="Text numberOfLines nested"
    decorators={[withStoryLabel('numberOfLines')]}
    args={{
      as: 'p',
      overflow: 'break',
      children:
        'The parent Text says this should only wrap to 2 lines, but the child which actually renders this text says it should wrap up to 3 lines.',
    }}
  >
    {(args) => (
      <VStack maxWidth={300} gap={3}>
        <TextBody as="p" numberOfLines={2}>
          <TextBody as="p" numberOfLines={3}>
            {args.children}
          </TextBody>
        </TextBody>
      </VStack>
    )}
  </Story>
</Canvas>

## Text Transform

<Canvas>
  <Story name="Uppercase" args={{ as: 'p', transform: 'uppercase' }}>
    {(args) => <TextBody {...args}>{args.children || 'uppercase'}</TextBody>}
  </Story>
  <Story name="Lowercase" args={{ as: 'p', transform: 'lowercase' }}>
    {(args) => <TextBody {...args}>{args.children || 'Lowercase'}</TextBody>}
  </Story>
  <Story name="Capitalize" args={{ as: 'p', transform: 'capitalize' }}>
    {(args) => <TextBody {...args}>{args.children || 'capitalize'}</TextBody>}
  </Story>
</Canvas>

## Sup

<Canvas>
  <Story name="Sup" args={{ as: 'sup' }}>
    <TextBody as="sup">sup</TextBody>
  </Story>
</Canvas>

## Sub

<Canvas>
  <Story name="Sub" args={{ as: 'sub' }}>
    <TextBody as="sub">sub</TextBody>
  </Story>
</Canvas>

## Strong

<Canvas>
  <Story name="Strong" args={{ as: 'strong' }}>
    <TextBody as="strong">strong</TextBody>
  </Story>
</Canvas>

## Bold

<Canvas>
  <Story name="Bold" args={{ as: 'b' }}>
    <TextBody as="b">b</TextBody>
  </Story>
</Canvas>

## Description Elements

<Canvas>
  <Story name="Description dl > dd">
    <dl>
      <TextBody as="dt">Coffee</TextBody>
      <TextBody as="dd">Black hot drink</TextBody>
      <div>
        <TextBody as="dt">Milk</TextBody>
        <TextBody as="dd">White cold drink</TextBody>
      </div>
    </dl>
  </Story>
</Canvas>

## API

<ArgsTable />
