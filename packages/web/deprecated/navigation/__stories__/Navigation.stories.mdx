import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { withStoryLabel } from '../../../storybook-decorators/withStoryLabel';

import { Navigation } from '../Navigation';
import {
  NoTabsNoTitle,
  TabsNoTitle,
  TitleNoTabs,
  TabsAndDisplayTitle,
  SidebarCollapsed,
  SidebarWithSections,
} from './NavigationStorySetup';

<Meta title="Core Components/Deprecated/Navigation" component={Navigation} />

# Navigation

<Canvas isColumn withToolbar>
  <Story name="NO Tabs NO displayTitle" decorators={[withStoryLabel('NO Tabs NO displayTitle')]}>
    <NoTabsNoTitle />
  </Story>
</Canvas>

<Canvas isColumn withToolbar>
  <Story
    name="With Tabs NO displayTitle"
    decorators={[withStoryLabel('With Tabs NO displayTitle')]}
    parameters={{
      a11y: {
        config: {
          rules: [{ id: 'aria-required-children', enabled: false }],
        },
      },
    }}
  >
    <TabsNoTitle />
  </Story>
</Canvas>

<Canvas isColumn withToolbar>
  <Story
    name="NO Tabs JUST displayTitle"
    decorators={[withStoryLabel('NO Tabs JUST displayTitle')]}
    parameters={{
      a11y: {
        <!-- This story is deprecated so disabling all a11y checks -->
        disable: true
      },
    }}
  >
    <TitleNoTabs />
  </Story>
</Canvas>

<Canvas isColumn withToolbar>
  <Story
    name="With Tabs AND displayTitle"
    decorators={[withStoryLabel('With Tabs AND displayTitle')]}
    parameters={{
      a11y: {
        config: {
          rules: [{ id: 'aria-required-children', enabled: false }],
        },
      },
    }}
  >
    <TabsAndDisplayTitle />
  </Story>
</Canvas>

<Canvas isColumn withToolbar>
  <Story name="Sidebar Collapsed" decorators={[withStoryLabel('Sidebar Collapsed')]}>
    <SidebarCollapsed />
  </Story>
</Canvas>

<Canvas isColumn withToolbar>
  <Story name="Sidebar With Sections" decorators={[withStoryLabel('Sidebar With Sections')]}>
    <SidebarWithSections />
  </Story>
</Canvas>

## API

<ArgsTable />
