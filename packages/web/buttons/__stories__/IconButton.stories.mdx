import { withStoryLabel } from '../../storybook-decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';

import { IconButton } from '../IconButton';

<Meta title="Core Components/Buttons/IconButton" component={IconButton} />

# IconButton

<Canvas isColumn withToolbar>
  <Story
    name="Default"
    decorators={[withStoryLabel('Default')]}
    args={{ name: 'arrowsHorizontal', accessibilityLabel: 'Horizontal arrows' }}
  >
    {(args) => (
      <>
        <IconButton {...args} />
        <IconButton {...args} to="/" />
      </>
    )}
  </Story>
</Canvas>

# Compact

<Canvas isColumn withToolbar>
  <Story
    name="Without Compact Styles"
    decorators={[withStoryLabel('Compact')]}
    args={{ name: 'arrowsHorizontal', compact: false, accessibilityLabel: 'Horizontal arrows' }}
  >
    {(args) => (
      <>
        <IconButton {...args} />
        <IconButton {...args} to="/" />
      </>
    )}
  </Story>
</Canvas>

## States

<Canvas withToolbar>
  <Story
    name="Primary disabled"
    decorators={[withStoryLabel('Disabled')]}
    args={{
      disabled: true,
      name: 'arrowsHorizontal',
      variant: 'primary',
      accessibilityLabel: 'Horizontal arrows',
    }}
  >
    {(args) => <IconButton {...args} />}
  </Story>
  <Story
    name="Secondary disabled"
    decorators={[withStoryLabel('Secondary disabled')]}
    args={{
      disabled: true,
      name: 'arrowsHorizontal',
      accessibilityLabel: 'Horizontal arrows',
    }}
  >
    {(args) => (
      <div>
        <IconButton {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Variants

<Canvas withToolbar>
  <Story
    name="Primary"
    decorators={[withStoryLabel('Primary')]}
    args={{
      variant: 'primary',
      name: 'arrowsHorizontal',
      accessibilityLabel: 'Horizontal arrows',
    }}
  >
    {(args) => (
      <div>
        <IconButton {...args} />
        <br />
        <IconButton {...args} transparent />
      </div>
    )}
  </Story>
  <Story
    name="Secondary"
    decorators={[withStoryLabel('Secondary')]}
    args={{
      variant: 'secondary',
      name: 'arrowsHorizontal',
      accessibilityLabel: 'Horizontal arrows',
    }}
  >
    {(args) => (
      <div>
        <IconButton {...args} />
        <br />
        <IconButton {...args} transparent />
      </div>
    )}
  </Story>
</Canvas>

## API

<ArgsTable />
