{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/web/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "command": "rm -rf esm && babel ./src --out-dir esm --extensions .ts,.tsx,.js,.jsx --copy-files --no-copy-ignored"
    },
    "build-css": {
      "command": "tsx ./build-css.ts",
      "dependsOn": [
        "build"
      ],
      "options": {
        "cwd": "packages/web"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@cbhq/mono-tasks:test"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "dev",
      "configurations": {
        "dev": {
          "command": "tsc --build --pretty --verbose"
        },
        "prod": {
          "command": "tsc --build ./tsconfig.build.json --pretty --verbose"
        }
      }
    },
    "audit-a11y": {
      "executor": "@cbhq/ui-scorecard:audit-a11y",
      "dependsOn": [
        "test"
      ],
      "options": {
        "eventProjectName": "cds_web",
        "platform": "web"
      }
    },
    "publish-figma": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "figma connect publish --token $FIGMA_CODE_CONNECT_TOKEN"
        ],
        "cwd": "packages/web"
      }
    }
  }
}
