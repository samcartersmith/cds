import { useToggler } from '@cbhq/cds-common/hooks/useToggler';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { DarkStoryContainer } from '../../storybook-decorators/StoryContainer';
import { withStoryLabel } from '../../storybook-decorators/withStoryLabel';

import { useCheckboxGroupState } from '../../hooks/useCheckboxGroupState';
import { Box } from '../../layout/Box';
import { ThemeProvider } from '../../system/ThemeProvider';
import { TextHeadline } from '../../typography/TextHeadline';
import { Checkbox } from '../Checkbox';
import { CheckboxGroup } from '../CheckboxGroup';

<Meta title="Core Components/Checkbox" component={Checkbox} />

# Checkbox

<Canvas isColumn withToolbar>
  <Story name="Normal">
    {() => {
      const [checked, { toggle }] = useToggler();
      return (
        <Checkbox name="normal-checkbox" checked={checked} onChange={toggle}>
          Normal
        </Checkbox>
      );
    }}
  </Story>
  <Story name="Dense">
    {() => {
      const [checked, { toggle }] = useToggler();
      return (
        <ThemeProvider scale="xSmall">
          <Checkbox name="dense-checkbox" checked={checked} onChange={toggle}>
            Dense
          </Checkbox>
        </ThemeProvider>
      );
    }}
  </Story>
  <Story name="Dark normal" decorators={[DarkStoryContainer]}>
    {() => {
      const [checked, { toggle }] = useToggler();
      return (
        <ThemeProvider spectrum="dark">
          <Checkbox checked={checked} onChange={toggle}>
            Normal
          </Checkbox>
        </ThemeProvider>
      );
    }}
  </Story>
  <Story name="No label" decorators={[withStoryLabel('No label')]}>
    {() => {
      const [checked, { toggle }] = useToggler();
      return <Checkbox checked={checked} onChange={toggle} />;
    }}
  </Story>
</Canvas>

## States

<Canvas withToolbar>
  <Story name="Disabled unselected">
    <Checkbox disabled>Disabled unselected</Checkbox>
  </Story>
  <Story name="Disabled selected">
    <Checkbox checked disabled>
      Disabled selected
    </Checkbox>
  </Story>
  <Story name="Read-only unselected">
    <Checkbox readOnly>Read-only unselected</Checkbox>
  </Story>
  <Story name="Read-only selected">
    <Checkbox checked readOnly>
      Read-only selected
    </Checkbox>
  </Story>
  <Story name="Multi-line label">
    <Box width="250px">
      <Checkbox>
        This checkbox has a multi-line label. The checkbox and label should align at the top.
      </Checkbox>
    </Box>
  </Story>
</Canvas>

# Checkbox Group

export const options = {
  'fish-taco': 'Fish tacos',
  puttanesca: 'Spaghetti alla puttanesca',
  'hamachi-salad': 'Hamachi salad',
};

export const optionValues = Object.keys(options);

<Canvas isColumn withToolbar>
  <Story name="Group">
    {() => {
      const [selectedValues, { toggle }] = useCheckboxGroupState(optionValues);
      return (
        <>
          <TextHeadline as="h1" id="order-dinner-label-no-select-all">
            Checkbox Group Default
          </TextHeadline>
          <CheckboxGroup
            aria-labelledby="order-dinner-label-no-select-all"
            name="checkbox-group-default"
            selectedValues={selectedValues}
            onChange={toggle}
          >
            {Object.entries(options).map(([value, label]) => (
              <Checkbox key={value} value={value}>
                {label}
              </Checkbox>
            ))}
          </CheckboxGroup>
        </>
      );
    }}
  </Story>
  <Story name="Custom styles checkbox group">
    {() => {
      const [selectedValues, { toggle }] = useCheckboxGroupState(optionValues);
      return (
        <>
          <TextHeadline as="h1" id="custom-styles-checkbox-group">
            Custom checkbox group
          </TextHeadline>
          <CheckboxGroup
            aria-labelledby="custom-styles-checkbox-group"
            name="checkbox-group-custom-styles"
          >
            {Object.entries(options).map(([value, label]) => (
              <div key={value} style={{ padding: '6px 8px' }}>
                <Checkbox value={value} checked={selectedValues.has(value)} onChange={toggle}>
                  {label}
                </Checkbox>
              </div>
            ))}
          </CheckboxGroup>
        </>
      );
    }}
  </Story>
</Canvas>

## API

<ArgsTable />
