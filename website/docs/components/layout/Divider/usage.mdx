import { ApiDetails } from '@cbhq/cds-website/components/ApiDetails';

```jsx live
<VStack>
  <Box height={100} width={100} />
  <Divider direction="horizontal" />
  <Box height={100} width={100} />
</VStack>
```

```jsx live
<HStack>
  <Box height={100} width={100} />
  <Divider direction="vertical" />
  <Box height={100} width={100} />
</HStack>
```

## Props

### direction

The direction to render the divider line.

<ApiDetails
  componentName="Divider"
  propertyName="direction"
  required={false}
  defaultValue="horizontal"
  mobileOptions={['horizontal', 'vertical']}
  webOptions={['horizontal', 'vertical']}
/>

## color

Color of the divider line.

<ApiDetails
  componentName="Box"
  propertyName="color"
  required={false}
  defaultValue="line"
  mobileOptions={['line', 'lineHeavy']}
  webOptions={['line', 'lineHeavy']}
/>
