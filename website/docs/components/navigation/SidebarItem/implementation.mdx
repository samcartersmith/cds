import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/navigation/SidebarItem](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/navigation/SidebarItem.tsx)

<ComponentDetails componentName="SidebarItem" templateName="intro" />

## Usage

<ComponentDetails componentName="SidebarItem" templateName="usage" />

## Accessibility

<ComponentDetails componentName="SidebarItem" templateName="a11y" />

## Props

### active <Badge variant="web" />

Use the active prop to identify the current page

<ApiDetails
  componentName="SidebarItem"
  propertyName="active"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-active" />

### collapsed <Badge variant="web" />

Use collapsed to show only the logo

<ApiDetails
  componentName="SidebarItem"
  propertyName="collapsed"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-collapsed" />

### icon <Badge variant="web" />

The Navigation Icon this item represents

<ApiDetails
  componentName="SidebarItem"
  propertyName="icon"
  required={true}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[
    'account',
    'addPeople',
    'appSwitcher',
    'application',
    'bell',
    'caretUp',
    'cash',
    'chartBar',
    'chartCandles',
    'chartPie',
    'dashboard',
    'defi',
    'directDeposit',
    'documentation',
    'earn',
    'eye',
    'gab',
    'giftBox',
    'home',
    'invoice',
    'moreVertical',
    'newsfeed',
    'pay',
    'paymentCard',
    'planet',
    'scanQrCode',
    'securityShield',
    'settings',
    'shoppingCart',
    'support',
    'tag',
    'taxes',
    'trading',
    'trophyCup',
    'wallet',
  ]}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-icon" />

### onPress <Badge variant="web" />

Handle the press events, ie () => navigate(/trade)

<ApiDetails
  componentName="SidebarItem"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['() => void']}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-onPress" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="SidebarItem"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-testID" />

### title <Badge variant="web" />

The title of the page this item represents

<ApiDetails
  componentName="SidebarItem"
  propertyName="title"
  required={true}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SidebarItem" templateName="prop-title" />

<ComponentDetails componentName="SidebarItem" templateName="outro" />
