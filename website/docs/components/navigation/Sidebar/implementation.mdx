import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/navigation/Sidebar](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/navigation/Sidebar.tsx)

<ComponentDetails componentName="Sidebar" templateName="intro" />

## Usage

<ComponentDetails componentName="Sidebar" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Sidebar" templateName="a11y" />

## Props

### autoCollapse <Badge variant="web" />

When set, the sidebar will auto collapse at or below the tablet breakpoint (currently 768px)

<ApiDetails
  componentName="Sidebar"
  propertyName="autoCollapse"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Sidebar" templateName="prop-autoCollapse" />

### children <Badge variant="web" />

<ApiDetails
  componentName="Sidebar"
  propertyName="children"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[
    'ReactNode[]',
    'ReactNode[] & ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNode[] & ReactNodeArray',
    'ReactNode[] & ReactPortal',
    'ReactNode[] & false',
    'ReactNode[] & number',
    'ReactNode[] & string',
    'ReactNode[] & true',
  ]}
/>

<ComponentDetails componentName="Sidebar" templateName="prop-children" />

### collapsed <Badge variant="web" />

Use collapsed to show only the logo

<ApiDetails
  componentName="Sidebar"
  propertyName="collapsed"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Sidebar" templateName="prop-collapsed" />

### logo <Badge variant="web" />

The logo to display

<ApiDetails
  componentName="Sidebar"
  propertyName="logo"
  required={true}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Sidebar" templateName="prop-logo" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Sidebar"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Sidebar" templateName="prop-testID" />

<ComponentDetails componentName="Sidebar" templateName="outro" />
