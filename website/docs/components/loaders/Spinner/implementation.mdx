import { ApiDetails } from '@cbhq/cds-website/components/ApiDetails';
import { Badge } from '@cbhq/cds-website/components/Badge';
import { ComponentDetails } from '@cbhq/cds-website/components/ComponentDetails';
import { RightSidebar } from '@cbhq/cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/loaders/Spinner](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/loaders/Spinner.tsx)

<ComponentDetails componentName="Spinner" templateName="intro" />

## Usage

<ComponentDetails componentName="Spinner" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Spinner" templateName="a11y" />

## Props

### color <Badge variant="web" />

Color of spinner.

<ApiDetails
  componentName="Spinner"
  propertyName="color"
  required={false}
  defaultValue="foregroundMuted"
  mobileOptions={[]}
  webOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
/>

<ComponentDetails componentName="Spinner" templateName="prop-color" />

### size <Badge variant="web" />

The pixel size of the spinner. Restricting spinner to have a n:n aspect ratio (square basically)

<ApiDetails
  componentName="Spinner"
  propertyName="size"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Spinner" templateName="prop-size" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Spinner"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Spinner" templateName="prop-testID" />

<ComponentDetails componentName="Spinner" templateName="outro" />
