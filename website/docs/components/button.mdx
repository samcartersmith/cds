---
id: button
title: Button
---

import { ApiDetails } from '@cbhq/cds-website/components/ApiDetails';
import { Badge } from '@cbhq/cds-website/components/Badge';
import { ComponentDetails } from '@cbhq/cds-website/components/ComponentDetails';

<ComponentDetails componentName="Button" templateName="intro" />

## Usage

<ComponentDetails componentName="Button" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Button" templateName="a11y" />

## Props

### accessibilityLabel

Full length accessibility label when the child text is not descriptive enough.

<ApiDetails
  componentName="Button"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Button" templateName="accessibilityLabelProp" />

### block

Change to block and expand to 100% of parent width.

<ApiDetails
  componentName="Button"
  propertyName="block"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Button" templateName="blockProp" />

### children

Children to render within the button.

<ApiDetails
  componentName="Button"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="Button" templateName="childrenProp" />

### compact

Reduce the inner padding within the button itself.

<ApiDetails
  componentName="Button"
  propertyName="compact"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Button" templateName="compactProp" />

### disabled

Mark the button as disabled.

<ApiDetails
  componentName="Button"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Button" templateName="disabledProp" />

### disableDebounce <Badge variant="mobile" />

React Native is historically trash at debouncing touch events. This can cause a lot of unwanted behavior such as double navigations where we push a screen onto the stack 2 times. Debouncing the event 500 miliseconds, but taking the leading event prevents this effect and the accidental double-tap.

<ApiDetails
  componentName="Button"
  propertyName="disableDebounce"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Button" templateName="disableDebounceProp" />

### feedback <Badge variant="mobile" />

Haptic feedback to trigger when being pressed.

<ApiDetails
  componentName="Button"
  propertyName="feedback"
  required={false}
  defaultValue="none"
  mobileOptions={['heavy', 'light', 'none', 'normal']}
  webOptions={[]}
/>

<ComponentDetails componentName="Button" templateName="feedbackProp" />

### focusable <Badge variant="web" />

When an element is `disabled`, it may still be `focusable`. It works similarly to `readOnly` on form elements. In this case, only `aria-disabled` will be set.

<ApiDetails
  componentName="Button"
  propertyName="focusable"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Button" templateName="focusableProp" />

### loading

Mark the button as loading and display a spinner.

<ApiDetails
  componentName="Button"
  propertyName="loading"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Button" templateName="loadingProp" />

### name

Uniquely identify the button within a form.

<ApiDetails
  componentName="Button"
  propertyName="name"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Button" templateName="nameProp" />

### onPress

Callback fired when the element is pressed.

<ApiDetails
  componentName="Button"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void', 'null']}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="Button" templateName="onPressProp" />

### testID

Testing purposes.

<ApiDetails
  componentName="Button"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Button" templateName="testIDProp" />

### variant

Toggle design and visual variants.

<ApiDetails
  componentName="Button"
  propertyName="variant"
  required={false}
  defaultValue="primary"
  mobileOptions={['negative', 'positive', 'primary', 'secondary']}
  webOptions={['negative', 'positive', 'primary', 'secondary']}
/>

<ComponentDetails componentName="Button" templateName="variantProp" />

<ComponentDetails componentName="Button" templateName="outro" />
