import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-mobile/visualizations/InteractiveSparkline](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/mobile/visualizations/InteractiveSparkline.tsx)

<ComponentDetails componentName="InteractiveSparkline" templateName="intro" />

## Usage

<ComponentDetails componentName="InteractiveSparkline" templateName="usage" />

## Accessibility

<ComponentDetails componentName="InteractiveSparkline" templateName="a11y" />

## Props

### compact <Badge variant="mobile" />

Show the chart in compact height

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-compact" />

### data <Badge variant="mobile" />

Chart data bucketed by Period. Period is a string key

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="data"
  required={false}
  defaultValue=""
  mobileOptions={['Record<Period, ChartData>']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-data" />

### defaultPeriod <Badge variant="mobile" />

default period value that the chart will use

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="defaultPeriod"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-defaultPeriod" />

### disableScrubbing <Badge variant="mobile" />

Disables the scrub user interaction from the chart

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="disableScrubbing"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-disableScrubbing" />

### fallback <Badge variant="mobile" />

Fallback shown in the chart when data is not available. This is usually a loading state.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="fallback"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-fallback" />

### fill <Badge variant="mobile" />

Adds an area fill to the Sparkline

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="fill"
  required={false}
  defaultValue="true for frontier false otherwise"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-fill" />

### formatAmount <Badge variant="mobile" />

function used to format the amount of money used in the minMaxLabel

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="formatAmount"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-formatAmount" />

### formatDate <Badge variant="mobile" />

function used to format the date that is shown in the bottom of the chart as the user scrubs

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="formatDate"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-formatDate" />

### formatHoverDate <Badge variant="mobile" />

Formats the date above the chart as you scrub. Omit this if you dont want to show the date as the user scrubs

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="formatHoverDate"
  required={false}
  defaultValue=""
  mobileOptions={['(date: Date, period: Period) => string']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-formatHoverDate" />

### headerNode <Badge variant="mobile" />

Adds a header node above the chart. It will be placed next to the period selector on web.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="headerNode"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-headerNode" />

### hideMinMaxLabel <Badge variant="mobile" />

Hides the min and max label

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="hideMinMaxLabel"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-hideMinMaxLabel" />

### hidePeriodSelector <Badge variant="mobile" />

Hides the period selector at the bottom of the chart

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="hidePeriodSelector"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-hidePeriodSelector" />

### onPeriodChanged <Badge variant="mobile" />

Callback when the user selects a new period.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="onPeriodChanged"
  required={false}
  defaultValue=""
  mobileOptions={['(period: Period) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-onPeriodChanged" />

### onScrub <Badge variant="mobile" />

Callback used when the user is scrubbing. This will be called for every data point change.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="onScrub"
  required={false}
  defaultValue=""
  mobileOptions={['(params: ChartScrubParams<Period>) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-onScrub" />

### onScrubEnd <Badge variant="mobile" />

Callback when a user finishes scrubbing

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="onScrubEnd"
  required={false}
  defaultValue=""
  mobileOptions={['() => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-onScrubEnd" />

### onScrubStart <Badge variant="mobile" />

Callback when the user starts scrubbing

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="onScrubStart"
  required={false}
  defaultValue=""
  mobileOptions={['() => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-onScrubStart" />

### periods <Badge variant="mobile" />

A list of periods that the chart will use. label is what is shown in the bottom of the chart and the value is the key.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="periods"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-periods" />

### strokeColor <Badge variant="mobile" />

Color of the line

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="strokeColor"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-strokeColor" />

### yAxisScalingFactor <Badge variant="mobile" />

Scales the sparkline to show more or less variance. Use a number less than 1 for less variance and a number greater than 1 for more variance. If you use a number greater than 1 it may clip the boundaries of the sparkline.

<ApiDetails
  componentName="InteractiveSparkline"
  propertyName="yAxisScalingFactor"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={[]}
/>

<ComponentDetails componentName="InteractiveSparkline" templateName="prop-yAxisScalingFactor" />

<ComponentDetails componentName="InteractiveSparkline" templateName="outro" />
