import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/visualizations/SparklineInteractive](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/visualizations/SparklineInteractive.tsx)

<ComponentDetails componentName="SparklineInteractive" templateName="intro" />

## Usage

<ComponentDetails componentName="SparklineInteractive" templateName="usage" />

## Accessibility

<ComponentDetails componentName="SparklineInteractive" templateName="a11y" />

## Props

### compact

Show the chart in compact height

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-compact" />

### data

Chart data bucketed by Period. Period is a string key

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="data"
  required={false}
  defaultValue=""
  mobileOptions={['Record<Period, ChartData>']}
  webOptions={['Record<Period, ChartData>']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-data" />

### defaultPeriod

default period value that the chart will use

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="defaultPeriod"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-defaultPeriod" />

### disableScrubbing

Disables the scrub user interaction from the chart

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="disableScrubbing"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-disableScrubbing" />

### fallback

Fallback shown in the chart when data is not available. This is usually a loading state.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="fallback"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-fallback" />

### fallbackType

If you use the default fallback then this specifies if the fallback line is decreasing or increasing

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="fallbackType"
  required={false}
  defaultValue=""
  mobileOptions={['negative', 'positive']}
  webOptions={['negative', 'positive']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-fallbackType" />

### fill

Adds an area fill to the Sparkline

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="fill"
  required={false}
  defaultValue="true for frontier false otherwise"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-fill" />

### formatDate

function used to format the date that is shown in the bottom of the chart as the user scrubs

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="formatDate"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-formatDate" />

### formatHoverDate

Formats the date above the chart as you scrub. Omit this if you dont want to show the date as the user scrubs

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="formatHoverDate"
  required={false}
  defaultValue=""
  mobileOptions={['(date: Date, period: Period) => string']}
  webOptions={['(date: Date, period: Period) => string']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-formatHoverDate" />

### formatMinMaxLabel <Badge variant="mobile" />

function used to format the amount of money used in the minMaxLabel

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="formatMinMaxLabel"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-formatMinMaxLabel" />

### headerNode

Adds a header node above the chart. It will be placed next to the period selector on web.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="headerNode"
  required={false}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-headerNode" />

### hideMinMaxLabel <Badge variant="mobile" />

Hides the min and max label

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="hideMinMaxLabel"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-hideMinMaxLabel" />

### hidePeriodSelector

Hides the period selector at the bottom of the chart

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="hidePeriodSelector"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-hidePeriodSelector" />

### onPeriodChanged

Callback when the user selects a new period.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="onPeriodChanged"
  required={false}
  defaultValue=""
  mobileOptions={['(period: Period) => void']}
  webOptions={['(period: Period) => void']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-onPeriodChanged" />

### onScrub

Callback used when the user is scrubbing. This will be called for every data point change.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="onScrub"
  required={false}
  defaultValue=""
  mobileOptions={['(params: ChartScrubParams<Period>) => void']}
  webOptions={['(params: ChartScrubParams<Period>) => void']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-onScrub" />

### onScrubEnd

Callback when a user finishes scrubbing

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="onScrubEnd"
  required={false}
  defaultValue=""
  mobileOptions={['() => void']}
  webOptions={['() => void']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-onScrubEnd" />

### onScrubStart

Callback when the user starts scrubbing

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="onScrubStart"
  required={false}
  defaultValue=""
  mobileOptions={['() => void']}
  webOptions={['() => void']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-onScrubStart" />

### periods

A list of periods that the chart will use. label is what is shown in the bottom of the chart and the value is the key.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="periods"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-periods" />

### strokeColor

Color of the line\*

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="strokeColor"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-strokeColor" />

### yAxisScalingFactor

Scales the sparkline to show more or less variance. Use a number less than 1 for less variance and a number greater than 1 for more variance. If you use a number greater than 1 it may clip the boundaries of the sparkline.

<ApiDetails
  componentName="SparklineInteractive"
  propertyName="yAxisScalingFactor"
  required={false}
  defaultValue=""
  mobileOptions={['number']}
  webOptions={['number']}
/>

<ComponentDetails componentName="SparklineInteractive" templateName="prop-yAxisScalingFactor" />

<ComponentDetails componentName="SparklineInteractive" templateName="outro" />
