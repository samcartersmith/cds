import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/visualizations/ProgressBar](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/visualizations/ProgressBar.tsx)

<ComponentDetails componentName="ProgressBar" templateName="intro" />

## Usage

<ComponentDetails componentName="ProgressBar" templateName="usage" />

## Accessibility

<ComponentDetails componentName="ProgressBar" templateName="a11y" />

## Props

### barColor

Custom bar color.

<ApiDetails
  componentName="ProgressBar"
  propertyName="barColor"
  required={false}
  defaultValue="primary"
  mobileOptions={['foreground', 'negative', 'positive', 'primary']}
  webOptions={['foreground', 'negative', 'positive', 'primary']}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-barColor" />

### disabled

Toggle used to show a disabled bar and text

<ApiDetails
  componentName="ProgressBar"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-disabled" />

### endLabel

Label that is pinned to the end of the container or floated at the end of the filled in bar. If a render function is omitted then the value will be formatted as a percentage.

<ApiDetails
  componentName="ProgressBar"
  propertyName="endLabel"
  required={false}
  defaultValue=""
  mobileOptions={[
    '{ value: number; float?: boolean | undefined; render?: ((num: number, disabled?: boolean | undefined) => ReactNode) | undefined; }',
  ]}
  webOptions={[
    '{ value: number; float?: boolean | undefined; render?: ((num: number, disabled?: boolean | undefined) => ReactNode) | undefined; }',
  ]}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-endLabel" />

### labelPlacement

Position of label relative to the bar

<ApiDetails
  componentName="ProgressBar"
  propertyName="labelPlacement"
  required={false}
  defaultValue="above"
  mobileOptions={['above', 'below', 'beside']}
  webOptions={['above', 'below', 'beside']}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-labelPlacement" />

### progress

Number between 0-1 representing the progress bar percentage

<ApiDetails
  componentName="ProgressBar"
  propertyName="progress"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-progress" />

### startLabel

Label that is pinned to the start of the container

<ApiDetails
  componentName="ProgressBar"
  propertyName="startLabel"
  required={false}
  defaultValue=""
  mobileOptions={[
    '{ value: number; render?: ((num: number, disabled?: boolean | undefined) => ReactNode) | undefined; }',
  ]}
  webOptions={[
    '{ value: number; render?: ((num: number, disabled?: boolean | undefined) => ReactNode) | undefined; }',
  ]}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-startLabel" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="ProgressBar"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-testID" />

### weight

Toggle used to show a thicker bar

<ApiDetails
  componentName="ProgressBar"
  propertyName="weight"
  required={false}
  defaultValue="normal"
  mobileOptions={['heavy', 'normal']}
  webOptions={['heavy', 'normal']}
/>

<ComponentDetails componentName="ProgressBar" templateName="prop-weight" />

<ComponentDetails componentName="ProgressBar" templateName="outro" />
