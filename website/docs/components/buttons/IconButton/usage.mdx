Icon buttons must have a name (which determines the icon), and can use variants to denote intent and importance.

```jsx
<IconButton name="document" onPress={console.log} />
<IconButton name="allTimeHigh" onPress={console.log} variant="secondary" />
```
