import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/overlays/PopoverMenu](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/overlays/PopoverMenu.tsx)

<ComponentDetails componentName="PopoverMenu" templateName="intro" />

## Usage

<ComponentDetails componentName="PopoverMenu" templateName="usage" />

## Accessibility

<ComponentDetails componentName="PopoverMenu" templateName="a11y" />

## Props

### accessibilityHint <Badge variant="web" />

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="PopoverMenu"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-accessibilityHint" />

### accessibilityLabel <Badge variant="web" />

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="PopoverMenu"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-accessibilityLabel" />

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="PopoverMenu"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-accessibilityLabelledBy" />

### closeMenu <Badge variant="web" />

Callback that unMounts the Menu

<ApiDetails
  componentName="PopoverMenu"
  propertyName="closeMenu"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-closeMenu" />

### disabled <Badge variant="web" />

Disable interaction with the Menu and trigger

<ApiDetails
  componentName="PopoverMenu"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-disabled" />

### disablePortal <Badge variant="web" />

Does not render the PopoverMenu inside of a portal (react-dom createPortal). Portal is automatically disabled for SSR

<ApiDetails
  componentName="PopoverMenu"
  propertyName="disablePortal"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-disablePortal" />

### flush <Badge variant="web" />

When true, the PopoverMenu trigger will fill all available horizontal space

<ApiDetails
  componentName="PopoverMenu"
  propertyName="flush"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-flush" />

### maxHeight <Badge variant="web" />

Can optionally pass a maxHeight.

<ApiDetails
  componentName="PopoverMenu"
  propertyName="maxHeight"
  required={false}
  defaultValue="300"
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-maxHeight" />

### maxWidth <Badge variant="web" />

Maximum width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="PopoverMenu"
  propertyName="maxWidth"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-maxWidth" />

### minWidth <Badge variant="web" />

Minimum width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="PopoverMenu"
  propertyName="minWidth"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-minWidth" />

### onBlur <Badge variant="web" />

Callback that fires when PopoverMenu or trigger are blurred

<ApiDetails
  componentName="PopoverMenu"
  propertyName="onBlur"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['NoopFn']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-onBlur" />

### onChange <Badge variant="web" />

Callback that is fired whenever a select option is selected

<ApiDetails
  componentName="PopoverMenu"
  propertyName="onChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['(newValue: string) => void', 'SetState<string>']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-onChange" />

### openMenu <Badge variant="web" />

Callback that mounts the Menu

<ApiDetails
  componentName="PopoverMenu"
  propertyName="openMenu"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-openMenu" />

### popoverPositionConfig <Badge variant="web" /> <Badge variant="internal" order={2} />

Pass a custom config for the PopoverMenu position relative to the trigger

<ApiDetails
  componentName="PopoverMenu"
  propertyName="popoverPositionConfig"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['PopoverPositionConfig']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-popoverPositionConfig" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="PopoverMenu"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-testID" />

### value <Badge variant="web" />

Default selected value, or preselected value

<ApiDetails
  componentName="PopoverMenu"
  propertyName="value"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-value" />

### visible <Badge variant="web" />

Whether Menu is mounted or dismounted

<ApiDetails
  componentName="PopoverMenu"
  propertyName="visible"
  required={true}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-visible" />

### width <Badge variant="web" />

Width of input as a percentage string or number converted to pixels.

<ApiDetails
  componentName="PopoverMenu"
  propertyName="width"
  required={false}
  defaultValue="100%"
  mobileOptions={[]}
  webOptions={['`${number}%`', 'number']}
/>

<ComponentDetails componentName="PopoverMenu" templateName="prop-width" />

<ComponentDetails componentName="PopoverMenu" templateName="outro" />
