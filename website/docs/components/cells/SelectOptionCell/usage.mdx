Supports the same props and usage as `ListCell`, but with spacing specific to usage in a `SelectInput` for web and `Menu` for mobile.

```jsx live
<SelectOptionCell value="25" key="25" title="25" description="BTC" />
```
