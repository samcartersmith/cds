import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/media/Avatar](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/media/Avatar.tsx)

<ComponentDetails componentName="Avatar" templateName="intro" />

## Usage

<ComponentDetails componentName="Avatar" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Avatar" templateName="a11y" />

## Props

### alt

This is the name associated with the entity in the Avatar. This is used in the image alt tag for accessibility.

<ApiDetails
  componentName="Avatar"
  propertyName="alt"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="Avatar" templateName="prop-alt" />

### borderColor

Adds a border to the Avatar

<ApiDetails
  componentName="Avatar"
  propertyName="borderColor"
  required={false}
  defaultValue=""
  mobileOptions={[
    'line',
    'lineHeavy',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
  ]}
  webOptions={['line', 'lineHeavy', 'negative', 'positive', 'primary', 'primaryWash', 'secondary']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-borderColor" />

### dangerouslySetClassName <Badge variant="web" /> <Badge variant="danger" order={2} />

Adds a className to the Avatar. If you pass in a className make sure your styles override the Avatar using the avatar class .cds-avatar like this: .my-class.cds-avatar

<ApiDetails
  componentName="Avatar"
  propertyName="dangerouslySetClassName"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-dangerouslySetClassName" />

### shape

Shape of Avatar

<ApiDetails
  componentName="Avatar"
  propertyName="shape"
  required={false}
  defaultValue=""
  mobileOptions={['circle', 'square']}
  webOptions={['circle', 'square']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-shape" />

### size

Size for a given avatar.

<ApiDetails
  componentName="Avatar"
  propertyName="size"
  required={false}
  defaultValue=""
  mobileOptions={['l', 'm', 'xl', 'xxl', 'xxxl']}
  webOptions={['l', 'm', 'xl', 'xxl', 'xxxl']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-size" />

### src

Absolute url to the image that should be shown in the Avatar. If no src is provided then a generic fallback image is used.

<ApiDetails
  componentName="Avatar"
  propertyName="src"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-src" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Avatar"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Avatar" templateName="prop-testID" />

<ComponentDetails componentName="Avatar" templateName="outro" />
