import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/tables/Table](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/tables/Table.tsx)

<ComponentDetails componentName="Table" templateName="intro" />

## Usage

<ComponentDetails componentName="Table" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Table" templateName="a11y" />

## Props

### border <Badge variant="web" />

When provided, well apply a border around the entire table

<ApiDetails
  componentName="Table"
  propertyName="border"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Table" templateName="prop-border" />

### children <Badge variant="web" />

Children are required, and should always include a TableBody. TableHead and TableFoot are both optional, and will magically flow into the correct place in the table (top/bottom)

<ApiDetails
  componentName="Table"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'ReactElement<TableSectionProps, string | JSXElementConstructor<any>>',
    'ReactElement<TableSectionProps, string | JSXElementConstructor<any>>[]',
  ]}
/>

<ComponentDetails componentName="Table" templateName="prop-children" />

### className <Badge variant="web" />

Linaria flavored className.

<ApiDetails
  componentName="Table"
  propertyName="className"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Table" templateName="prop-className" />

### variant <Badge variant="web" />

The variant prop allows clients to use a CDS approved style for their Table.

<ApiDetails
  componentName="Table"
  propertyName="variant"
  required={false}
  defaultValue="undefined"
  mobileOptions={[]}
  webOptions={['default', 'graph', 'ruled']}
/>

<ComponentDetails componentName="Table" templateName="prop-variant" />

<ComponentDetails componentName="Table" templateName="outro" />
