import { ApiDetails } from '@cbhq/cds-website/components/ApiDetails';
import { Badge } from '@cbhq/cds-website/components/Badge';
import { ComponentDetails } from '@cbhq/cds-website/components/ComponentDetails';
import { RightSidebar } from '@cbhq/cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/system/Interactable](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/system/Interactable.tsx)

<ComponentDetails componentName="Interactable" templateName="intro" />

## Usage

<ComponentDetails componentName="Interactable" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Interactable" templateName="a11y" />

## Props

### as <Badge variant="web" />

Element or component to render the container as.

<ApiDetails
  componentName="Interactable"
  propertyName="as"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'ComponentClass<any, any>',
    'FunctionComponent<any>',
    'a',
    'button',
    'div',
    'input',
    'label',
    'select',
    'span',
    'textarea',
  ]}
/>

<ComponentDetails componentName="Interactable" templateName="prop-as" />

### backgroundColor

Background color of the overlay (element being interacted with).

<ApiDetails
  componentName="Interactable"
  propertyName="backgroundColor"
  required={true}
  defaultValue=""
  mobileOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'foreground',
    'foregroundMuted',
    'line',
    'lineHeavy',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'primaryWash',
    'secondary',
    'secondaryForeground',
    'transparent',
  ]}
  webOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'foreground',
    'foregroundMuted',
    'line',
    'lineHeavy',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'primaryWash',
    'secondary',
    'secondaryForeground',
    'transparent',
  ]}
/>

<ComponentDetails componentName="Interactable" templateName="prop-backgroundColor" />

### block

Set element to block and expand to 100% width.

<ApiDetails
  componentName="Interactable"
  propertyName="block"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-block" />

### borderColor

Border color of the element being interacted with.

<ApiDetails
  componentName="Interactable"
  propertyName="borderColor"
  required={false}
  defaultValue=""
  mobileOptions={[
    'line',
    'lineHeavy',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
  webOptions={[
    'line',
    'lineHeavy',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
/>

<ComponentDetails componentName="Interactable" templateName="prop-borderColor" />

### borderRadius

Border radius of the element being interacted with.

<ApiDetails
  componentName="Interactable"
  propertyName="borderRadius"
  required={false}
  defaultValue=""
  mobileOptions={['badge', 'compact', 'input', 'pill', 'round', 'standard', 'tooltip']}
  webOptions={['badge', 'compact', 'input', 'pill', 'round', 'standard', 'tooltip']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-borderRadius" />

### borderWidth

Width of the border.

<ApiDetails
  componentName="Interactable"
  propertyName="borderWidth"
  required={false}
  defaultValue=""
  mobileOptions={['button', 'card', 'checkbox', 'focusRing', 'input', 'radio', 'sparkline']}
  webOptions={['button', 'card', 'checkbox', 'focusRing', 'input', 'radio', 'sparkline']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-borderWidth" />

### className <Badge variant="web" />

Apply class names to the outer container.

<ApiDetails
  componentName="Interactable"
  propertyName="className"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-className" />

### contentStyle <Badge variant="mobile" />

Apply animated styles to the inner container.

<ApiDetails
  componentName="Interactable"
  propertyName="contentStyle"
  required={false}
  defaultValue=""
  mobileOptions={['ViewStyle', 'false', 'null']}
  webOptions={[]}
/>

<ComponentDetails componentName="Interactable" templateName="prop-contentStyle" />

### disabled

Is the element currently disabled.

<ApiDetails
  componentName="Interactable"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-disabled" />

### elevation

Is the element elevated.

<ApiDetails
  componentName="Interactable"
  propertyName="elevation"
  required={false}
  defaultValue=""
  mobileOptions={['1', '2']}
  webOptions={['1', '2']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-elevation" />

### pressed

Is the element being pressed. Primarily a mobile feature, but can be used on the web.

<ApiDetails
  componentName="Interactable"
  propertyName="pressed"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-pressed" />

### style

<ApiDetails
  componentName="Interactable"
  propertyName="style"
  required={false}
  defaultValue=""
  mobileOptions={[
    '(false | Value | AnimatedInterpolation | WithAnimatedObject<ViewStyle> | null | undefined)[]',
  ]}
  webOptions={['CSSProperties']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-style" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Interactable"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-testID" />

### transparentWhileInactive

Mark the background and border as transparent until the element is interacted with (hovered, pressed, etc).

<ApiDetails
  componentName="Interactable"
  propertyName="transparentWhileInactive"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-transparentWhileInactive" />

### wrapWithLayeredElements <Badge variant="web" />

Inject and wrap the content with overlay and underlay elements.

<ApiDetails
  componentName="Interactable"
  propertyName="wrapWithLayeredElements"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Interactable" templateName="prop-wrapWithLayeredElements" />

<ComponentDetails componentName="Interactable" templateName="outro" />
