import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/system/Pressable](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/system/Pressable.tsx)

<ComponentDetails componentName="Pressable" templateName="intro" />

## Usage

<ComponentDetails componentName="Pressable" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Pressable" templateName="a11y" />

## Props

### accessibilityHint

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="Pressable"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-accessibilityHint" />

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="Pressable"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-accessibilityLabel" />

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="Pressable"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-accessibilityLabelledBy" />

### as <Badge variant="web" />

Element or component to render the container as.

<ApiDetails
  componentName="Pressable"
  propertyName="as"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['ComponentClass<any, any>', 'FunctionComponent<any>', 'a', 'button']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-as" />

### backgroundColor

Background color of the overlay (element being interacted with).

<ApiDetails
  componentName="Pressable"
  propertyName="backgroundColor"
  required={true}
  defaultValue=""
  mobileOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'foreground',
    'foregroundMuted',
    'line',
    'lineHeavy',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'primaryWash',
    'secondary',
    'secondaryForeground',
    'transparent',
  ]}
  webOptions={[
    'background',
    'backgroundAlternate',
    'backgroundOverlay',
    'foreground',
    'foregroundMuted',
    'line',
    'lineHeavy',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'primaryWash',
    'secondary',
    'secondaryForeground',
    'transparent',
  ]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-backgroundColor" />

### block

Set element to block and expand to 100% width.

<ApiDetails
  componentName="Pressable"
  propertyName="block"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-block" />

### borderColor

Border color of the element being interacted with.

<ApiDetails
  componentName="Pressable"
  propertyName="borderColor"
  required={false}
  defaultValue=""
  mobileOptions={[
    'line',
    'lineHeavy',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
  webOptions={[
    'line',
    'lineHeavy',
    'negative',
    'positive',
    'primary',
    'primaryWash',
    'secondary',
    'transparent',
  ]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-borderColor" />

### borderRadius

Border radius of the element being interacted with. Number should only be used if value comes from useButtonBorderRadius.

<ApiDetails
  componentName="Pressable"
  propertyName="borderRadius"
  required={false}
  defaultValue=""
  mobileOptions={[
    'badge',
    'compact',
    'input',
    'none',
    'number',
    'pill',
    'round',
    'standard',
    'tooltip',
  ]}
  webOptions={[
    'badge',
    'compact',
    'input',
    'none',
    'number',
    'pill',
    'round',
    'standard',
    'tooltip',
  ]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-borderRadius" />

### borderWidth

Width of the border.

<ApiDetails
  componentName="Pressable"
  propertyName="borderWidth"
  required={false}
  defaultValue=""
  mobileOptions={['button', 'card', 'checkbox', 'focusRing', 'input', 'none', 'radio', 'sparkline']}
  webOptions={['button', 'card', 'checkbox', 'focusRing', 'input', 'none', 'radio', 'sparkline']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-borderWidth" />

### className <Badge variant="web" />

Apply class names to the outer container.

<ApiDetails
  componentName="Pressable"
  propertyName="className"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-className" />

### contentStyle <Badge variant="mobile" />

Apply animated styles to the inner container.

<ApiDetails
  componentName="Pressable"
  propertyName="contentStyle"
  required={false}
  defaultValue=""
  mobileOptions={['ViewStyle', 'false', 'null']}
  webOptions={[]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-contentStyle" />

### disabled

Is the element currently disabled.

<ApiDetails
  componentName="Pressable"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-disabled" />

### disableDebounce <Badge variant="mobile" />

React Native is historically trash at debouncing touch events. This can cause a lot of unwanted behavior such as double navigations where we push a screen onto the stack 2 times. Debouncing the event 500 miliseconds, but taking the leading event prevents this effect and the accidental double-tap.

<ApiDetails
  componentName="Pressable"
  propertyName="disableDebounce"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-disableDebounce" />

### elevation

Is the element elevated.

<ApiDetails
  componentName="Pressable"
  propertyName="elevation"
  required={false}
  defaultValue=""
  mobileOptions={['0', '1', '2']}
  webOptions={['0', '1', '2']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-elevation" />

### feedback <Badge variant="mobile" />

Haptic feedback to trigger when being pressed.

<ApiDetails
  componentName="Pressable"
  propertyName="feedback"
  required={false}
  defaultValue="none"
  mobileOptions={['heavy', 'light', 'none', 'normal']}
  webOptions={[]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-feedback" />

### loading

Is the element currenty loading.

<ApiDetails
  componentName="Pressable"
  propertyName="loading"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-loading" />

### noScaleOnPress

Dont scale element on press.

<ApiDetails
  componentName="Pressable"
  propertyName="noScaleOnPress"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-noScaleOnPress" />

### onKeyPress <Badge variant="web" />

Callback fired when a key is pressed

<ApiDetails
  componentName="Pressable"
  propertyName="onKeyPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['KeyboardEventHandler<Element>']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-onKeyPress" />

### onPress

Callback fired when the element is pressed.

<ApiDetails
  componentName="Pressable"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void', 'null']}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-onPress" />

### onPressIn <Badge variant="mobile" />

Callback fired before `onPress` when button is pressed.

<ApiDetails
  componentName="Pressable"
  propertyName="onPressIn"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-onPressIn" />

### onPressOut <Badge variant="mobile" />

Callback fired before `onPress` when button is released.

<ApiDetails
  componentName="Pressable"
  propertyName="onPressOut"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void']}
  webOptions={[]}
/>

<ComponentDetails componentName="Pressable" templateName="prop-onPressOut" />

### style

<ApiDetails
  componentName="Pressable"
  propertyName="style"
  required={false}
  defaultValue=""
  mobileOptions={[
    '(state: PressableStateCallbackType) => StyleProp<ViewStyle>',
    'RecursiveArray<Falsy | ViewStyle | RegisteredStyle<ViewStyle>>',
    'RegisteredStyle<ViewStyle>',
    'ViewStyle',
    'false',
    'null',
  ]}
  webOptions={['CSSProperties']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-style" />

### tabIndex <Badge variant="web" />

Necessary to control roving tabindex for accessibility https://www.w3.org/TR/wai-aria-practices/#kbd_roving_tabindex

<ApiDetails
  componentName="Pressable"
  propertyName="tabIndex"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['number']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-tabIndex" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="Pressable"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-testID" />

### to <Badge variant="web" />

URL that this links to when pressed.

<ApiDetails
  componentName="Pressable"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-to" />

### transparentWhileInactive

Mark the background and border as transparent until the element is interacted with (hovered, pressed, etc).

<ApiDetails
  componentName="Pressable"
  propertyName="transparentWhileInactive"
  required={false}
  defaultValue=""
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-transparentWhileInactive" />

### wrapWithLayeredElements <Badge variant="web" />

Inject and wrap the content with overlay and underlay elements.

<ApiDetails
  componentName="Pressable"
  propertyName="wrapWithLayeredElements"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Pressable" templateName="prop-wrapWithLayeredElements" />

<ComponentDetails componentName="Pressable" templateName="outro" />
