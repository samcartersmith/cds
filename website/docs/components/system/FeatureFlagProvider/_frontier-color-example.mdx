```jsx live
function Example() {
  const [isEnabled, { toggle }] = useToggler(true);

  return (
    <FeatureFlagProvider frontierColor={isEnabled}>
      <ThemeProvider>
        <VStack gap={2}>
          <HStack gap={1} alignItems="center">
            <Switch onChange={toggle} checked={isEnabled} />
            <TextBody as="p">Toggle frontierColor</TextBody>
          </HStack>
          <Divider direction="horizontal" />
          <PaletteSheet />
        </VStack>
      </ThemeProvider>
    </FeatureFlagProvider>
  );
}
```
