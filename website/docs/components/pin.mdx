---
id: pin
title: Pin
---

import { ApiDetails } from '@cds/website/components/ApiDetails';
import { Badge } from '@cds/website/components/Badge';
import { ComponentDetails } from '@cds/website/components/ComponentDetails';

<ComponentDetails componentName="Pin" templateName="intro" />

## Props

### children

Content to render within the pinned element.

<ApiDetails
  componentName="Pin"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | ((props: any) => ReactElement<any, string | ... | (new (props: any) => Component<any, any, any>)> | null) | (new (props: any) => Component<any, any, any>)>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'ReactElement<any, string | ((props: any) => ReactElement<any, string | ... | (new (props: any) => Component<any, any, any>)> | null) | (new (props: any) => Component<any, any, any>)>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'null',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="Pin" templateName="children" />

### direction

Direction in which to pin the element.

<ApiDetails
  componentName="Pin"
  propertyName="direction"
  required={true}
  defaultValue=""
  mobileOptions={['all', 'bottom', 'left', 'right', 'top']}
  webOptions={['all', 'bottom', 'left', 'right', 'top']}
/>

<ComponentDetails componentName="Pin" templateName="direction" />

<ComponentDetails componentName="Pin" templateName="outro" />
