Icon buttons must have a name (which determines the icon), and can use variants to denote intent and importance.

```jsx
<IconButton name="documentHeavy" onPress={console.log} />
<IconButton name="starHeavy" onPress={console.log} variant="secondary" />
```
