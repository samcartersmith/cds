import { ApiDetails } from '@cbhq/cds-website/components/ApiDetails';
import { Badge } from '@cbhq/cds-website/components/Badge';
import { ComponentDetails } from '@cbhq/cds-website/components/ComponentDetails';
import { RightSidebar } from '@cbhq/cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/typography/Link](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/typography/Link.tsx)

<ComponentDetails componentName="Link" templateName="intro" />

## Usage

<ComponentDetails componentName="Link" templateName="usage" />

## Accessibility

<ComponentDetails componentName="Link" templateName="a11y" />

## Props

### accessibilityLabel

Full length accessibility label when the child text is not descriptive enough.

<ApiDetails
  componentName="Link"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Link" templateName="prop-accessibilityLabel" />

### children

Children to render within the link.

<ApiDetails
  componentName="Link"
  propertyName="children"
  required={true}
  defaultValue=""
  mobileOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
  webOptions={[
    'ReactElement<any, string | JSXElementConstructor<any>>',
    'ReactNodeArray',
    'ReactPortal',
    'false',
    'number',
    'string',
    'true',
    '{}',
  ]}
/>

<ComponentDetails componentName="Link" templateName="prop-children" />

### color

Color of the foreground text.

<ApiDetails
  componentName="Link"
  propertyName="color"
  required={false}
  defaultValue=""
  mobileOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
  webOptions={[
    'foreground',
    'foregroundMuted',
    'negative',
    'negativeForeground',
    'positive',
    'positiveForeground',
    'primary',
    'primaryForeground',
    'secondary',
    'secondaryForeground',
  ]}
/>

<ComponentDetails componentName="Link" templateName="prop-color" />

### forceOpenOutsideApp <Badge variant="mobile" />

Toggles whether the link should be opened outside or within app

<ApiDetails
  componentName="Link"
  propertyName="forceOpenOutsideApp"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Link" templateName="prop-forceOpenOutsideApp" />

### onPress

Callback fired when the element is clicked.

<ApiDetails
  componentName="Link"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void']}
  webOptions={['OnPress<HTMLAnchorElement>']}
/>

<ComponentDetails componentName="Link" templateName="prop-onPress" />

### openInNewWindow <Badge variant="web" />

If true, it opens the link in a new window. If false, it replaces the current screen with the link

<ApiDetails
  componentName="Link"
  propertyName="openInNewWindow"
  required={false}
  defaultValue="true"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="Link" templateName="prop-openInNewWindow" />

### preventRedirectionIntoApp <Badge variant="mobile" />

Toggles whether we allow users to go back to app when they are in an external browser

<ApiDetails
  componentName="Link"
  propertyName="preventRedirectionIntoApp"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Link" templateName="prop-preventRedirectionIntoApp" />

### readerMode <Badge variant="mobile" />

Toggles readerMode flag for web browser. Note: readerMode is only available on ios

<ApiDetails
  componentName="Link"
  propertyName="readerMode"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={[]}
/>

<ComponentDetails componentName="Link" templateName="prop-readerMode" />

### renderContainer <Badge variant="web" />

Callback for custom Link component

<ApiDetails
  componentName="Link"
  propertyName="renderContainer"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['(props: HTMLAttributes<HTMLAnchorElement>) => Element']}
/>

<ComponentDetails componentName="Link" templateName="prop-renderContainer" />

### testID

Used to locate this element in unit and end-to-end tests. This prop follows the precedent set by React Native Web which uses a testID prop to emit a data-testid attribute on the element

<ApiDetails
  componentName="Link"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Link" templateName="prop-testID" />

### to

URL that this link goes to when pressed.

<ApiDetails
  componentName="Link"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="Link" templateName="prop-to" />

### variant

Specify inner text type

<ApiDetails
  componentName="Link"
  propertyName="variant"
  required={false}
  defaultValue=""
  mobileOptions={[
    'body',
    'caption',
    'headline',
    'label1',
    'label2',
    'legal',
    'title1',
    'title2',
    'title3',
  ]}
  webOptions={[
    'body',
    'caption',
    'headline',
    'label1',
    'label2',
    'legal',
    'title1',
    'title2',
    'title3',
  ]}
/>

<ComponentDetails componentName="Link" templateName="prop-variant" />

<ComponentDetails componentName="Link" templateName="outro" />
