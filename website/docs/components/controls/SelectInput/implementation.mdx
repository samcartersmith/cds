import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/controls/SelectInput](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/controls/SelectInput.tsx)

<ComponentDetails componentName="SelectInput" templateName="intro" />

## Usage

<ComponentDetails componentName="SelectInput" templateName="usage" />

## Accessibility

<ComponentDetails componentName="SelectInput" templateName="a11y" />

## Props

### accessibilityHint

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="SelectInput"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-accessibilityHint" />

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label

<ApiDetails
  componentName="SelectInput"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-accessibilityLabel" />

### compact

Enables compact variation

<ApiDetails
  componentName="SelectInput"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-compact" />

### disabled

Toggles input interactability and opacity

<ApiDetails
  componentName="SelectInput"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-disabled" />

### helperText

For cases where label is not enough information to describe what the text input is for. Can also be used for showing positive/negative messages

<ApiDetails
  componentName="SelectInput"
  propertyName="helperText"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-helperText" />

### label

Optional string placed above the input (or within if compact is enabled) to indicate purpose of the input

<ApiDetails
  componentName="SelectInput"
  propertyName="label"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-label" />

### onChange <Badge variant="web" />

Callback that is fired whenever a select option is selected

<ApiDetails
  componentName="SelectInput"
  propertyName="onChange"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['(newValue: string) => void']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-onChange" />

### onPress

Event handler for when the Select Input trigger is pressed

<ApiDetails
  componentName="SelectInput"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={['NoopFn']}
  webOptions={['NoopFn']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-onPress" />

### placeholder

Placeholder text displayed inside of the input. Will be replaced if there is a value.

<ApiDetails
  componentName="SelectInput"
  propertyName="placeholder"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-placeholder" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="SelectInput"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-testID" />

### value

Pass a value that will prepopulate the select input. This will replace the placeholder text.

<ApiDetails
  componentName="SelectInput"
  propertyName="value"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-value" />

### variant

Determines the sentiment of the input. Because we allow startContent and endContent to be custom ReactNode, the content placed inside these slots will not change colors according to the variant. You will have to add that yourself

<ApiDetails
  componentName="SelectInput"
  propertyName="variant"
  required={false}
  defaultValue="foregroundMuted"
  mobileOptions={['foreground', 'foregroundMuted', 'negative', 'positive', 'primary']}
  webOptions={['foreground', 'foregroundMuted', 'negative', 'positive', 'primary']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-variant" />

### width

Width of input. The number is converted to percentage.

<ApiDetails
  componentName="SelectInput"
  propertyName="width"
  required={false}
  defaultValue="100"
  mobileOptions={['number']}
  webOptions={['number']}
/>

<ComponentDetails componentName="SelectInput" templateName="prop-width" />

<ComponentDetails componentName="SelectInput" templateName="outro" />
