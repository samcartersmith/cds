### Mobile Implementation

The mobile version of `Select` is quite different from web. When you interact with the trigger, it will toggle the visibility of a `Tray` which contains all the `SelectOption`s.

You can add a `title` as a prop to the`Tray`.

**Note**: `Tray` exposes a `closeTray` method as a prop to child functions. We recommend using the pattern shown below to control the visibility of the `Tray`.

#### Default Composition

```jsx
function DefaultSelectMobile() {
  const [isTrayVisible, { toggleOn, toggleOff }] = useToggler(false);
  const [value, setValue] = useState();
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <Select value={value} onPress={toggleOn}>
      {isTrayVisible && (
        <Tray title={trayTitle} onCloseComplete={toggleOff}>
          {({ closeTray }) =>
            options.map((option) => {
              return (
                <SelectOption
                  title={option.label}
                  key={option.value}
                  onPress={() => {
                    setValue(option.value);
                    closeTray();
                  }}
                  selected={value === option.value}
                />
              );
            })
          }
        </Tray>
      )}
    </Select>
  );
}
```

### Web Implementation

`Select` can take anything as a child, however, we strongly recommend that you use `SelectOption`. It has the same API as a `ListCell`, but with custom styles specific to usage within a `Select`. `SelectOption` also comes with a lot of baked in functionality, like keyboard navigation, accessibility properties, and focus behaviors.

#### Default Composition

```jsx live
function DefaultSelect() {
  const [value, setValue] = useState();
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select value={value} placeholder="Choose something" onChange={setValue}>
          {options.map((option) => (
            <SelectOption value={option} key={option} description={option} />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```

### Input Stack Options

The `Select` trigger can be customized similar to `TextInput`. These options are also available for mobile implementations.

#### Compact

When space is tight, this brings the label inside of the Input. Should be used with a `compact` `SelectOption`.

```jsx live
function CompactSelect() {
  const [value, setValue] = useState('54534');
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select compact label="$" value={value} placeholder="Choose something" onChange={setValue}>
          {options.map((option) => (
            <SelectOption value={option} key={option} compact title={option} description="BTC" />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```

#### Label

```jsx live
function SelectWithLabel() {
  const [value, setValue] = useState('54534');
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select
          label="Choose an amount"
          value={value}
          placeholder="Choose an option... "
          onChange={setValue}
        >
          {options.map((option) => (
            <SelectOption value={option} key={option} title={option} description="BTC" />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```

#### Helper Text

```jsx live
function SelectWithHelperText() {
  const [value, setValue] = useState();
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select
          value={value}
          helperText="This text should be helpful"
          placeholder="Choose something"
          onChange={setValue}
        >
          {options.map((option) => (
            <SelectOption value={option} key={option} title={option} description="BTC" />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```

### Variants

#### Positive

```jsx live
function SelectPositive() {
  const [value, setValue] = useState();
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select
          variant="positive"
          value={value}
          placeholder="Choose something"
          helperText="This text should be helpful"
          onChange={setValue}
        >
          {options.map((option) => (
            <SelectOption value={option} key={option} title={option} description="BTC" />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```

#### Negative

Useful for surfacing error states.

```jsx live
function SelectNegative() {
  const [value, setValue] = useState();
  const options = ['666', '2387', '4542', '54534', '5435', '77', '123', '4345345', '6544'];
  return (
    <ExampleWithThemeToggles>
      <VStack spacing={2} background minHeight={600}>
        <Select
          variant="negative"
          value={value}
          placeholder="Choose something"
          helperText="This text should be helpful"
          onChange={setValue}
        >
          {options.map((option) => (
            <SelectOption value={option} key={option} title={option} description="BTC" />
          ))}
        </Select>
      </VStack>
    </ExampleWithThemeToggles>
  );
}
```
