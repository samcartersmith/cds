import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-(web|mobile)/controls/SearchInput](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/controls/SearchInput.tsx)

<ComponentDetails componentName="SearchInput" templateName="intro" />

## Usage

<ComponentDetails componentName="SearchInput" templateName="usage" />

## Accessibility

<ComponentDetails componentName="SearchInput" templateName="a11y" />

## Props

### accessibilityHint

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="SearchInput"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-accessibilityHint" />

### accessibilityLabel

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="SearchInput"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-accessibilityLabel" />

### accessibilityLabelledBy

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="SearchInput"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-accessibilityLabelledBy" />

### compact

Enables compact variation

<ApiDetails
  componentName="SearchInput"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-compact" />

### disabled

Toggles input interactability and opacity

<ApiDetails
  componentName="SearchInput"
  propertyName="disabled"
  required={false}
  defaultValue="false"
  mobileOptions={['false', 'true']}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-disabled" />

### onChangeText

<ApiDetails
  componentName="SearchInput"
  propertyName="onChangeText"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[]}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-onChangeText" />

### onClear

<ApiDetails
  componentName="SearchInput"
  propertyName="onClear"
  required={false}
  defaultValue=""
  mobileOptions={['(event: GestureResponderEvent) => void']}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-onClear" />

### onSearch

<ApiDetails
  componentName="SearchInput"
  propertyName="onSearch"
  required={false}
  defaultValue=""
  mobileOptions={['(str: string) => void']}
  webOptions={['(text: string) => void']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-onSearch" />

### placeholder

Placeholder text displayed inside of the input. Will be replaced if there is a value.

<ApiDetails
  componentName="SearchInput"
  propertyName="placeholder"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-placeholder" />

### testID

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="SearchInput"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={['string']}
  webOptions={['string']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-testID" />

### width

Width of input as a percentage string.

<ApiDetails
  componentName="SearchInput"
  propertyName="width"
  required={false}
  defaultValue="100%"
  mobileOptions={['`${number}%`', 'number']}
  webOptions={['`${number}%` & string', 'number']}
/>

<ComponentDetails componentName="SearchInput" templateName="prop-width" />

<ComponentDetails componentName="SearchInput" templateName="outro" />
