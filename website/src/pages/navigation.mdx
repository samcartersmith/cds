import NavigationBarExample from ':cds-website/docs/components/navigation/NavigationBar/_example.mdx';
import SidebarExample from ':cds-website/docs/components/navigation/Sidebar/_example.mdx';
import PageTitleExample from ':cds-website/docs/components/navigation/PageTitle/_example.mdx';
import PageTitleSelect from ':cds-website/docs/components/navigation/PageTitle/_example_select.mdx';
import SidebarItemExample from ':cds-website/docs/components/navigation/SidebarItem/_example.mdx';

# New Navigation Components
These are the new navigation components. Read about the release process and motivation at [go/cds-nav-doc](http://go/cds-nav-doc).

## NavigationBar
Until the Navigation system and all it's parts are locked in, NavigationBar
is simply providing a bit of structure.
<NavigationBarExample />

> This API will likely get _much_ simpler when UA is locked in with something like:

```jsx
<NavigationBarExample
  pageTitle={pageTitle}
  avatarUrl={user.photoUrl}
  onBackPress={canGoBack && handleBack}
  onMenuOpen={handleMenuOpen}
  onAppSwitch={handleAppSwitch} />
```

## Sidebar
The Sidebar

<SidebarExample />

## SidebarItem
This component is to be used inside a Sidebar. Clients can use the `compact` prop for smaller breakpoints

<SidebarItemExample />

## PageTitle
This is the page title component

<PageTitle />

## PageTitle Select
You can easily compose a PageTitle select

<PageTitleSelectExample />

## Full Example

```jsx live
function fullExample() {
  const [activeIndex, setActiveIndex] = useState(0);
  const items = mocks.product.navigationItems;
  const {name, avatar} = mocks.users[1];

  return (
    <HStack>
      <Sidebar
        logo={
          <Link to="/">
            <LogoMark />
          </Link>
        }
      >
        {items.map((props, index) => (
          <SidebarItem
            key={`example--sidebar-item--${props.title}`}
            active={index === activeIndex}
            onPress={() => setActiveIndex(index)}
            {...props}
          />
        ))}
      </Sidebar>
      <VStack>
        <Box position="sticky" top={0}>
          <NavigationBar
            end={
              <HStack gap={1} alignItems="center">
                <IconButton name="bell" onPress={() => handlePress('Notifications')} />
                <Pressable backgroundColor="transparent" onPress={() => handlePress('Avatar group')}>
                  <HStack gap={1} alignItems="center">
                    <Avatar alt="Kat" src={avatar} size="xl" />
                    <TextHeadline as="h2">{name}</TextHeadline>
                  </HStack>
                </Pressable>
              </HStack>
            }
          >
            <PageTitle>Personal Portfolio</PageTitle>
          </NavigationBar>
        </Box>
        <VStack spacing={2}>
          <TextDisplay2 as="h2" spacingBottom={1}>
            {items[activeIndex].title}
          </TextDisplay2>
          {loremIpsum}
          {loremIpsum}
          {loremIpsum}
          {loremIpsum}
          {loremIpsum}
          {loremIpsum}
          {loremIpsum}
        </VStack>
      </VStack>
    </HStack>
  );
}
```