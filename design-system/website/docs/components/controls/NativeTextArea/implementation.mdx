import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/controls/NativeTextArea](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/controls/NativeTextArea.tsx)

<ComponentDetails componentName="NativeTextArea" templateName="intro" />

## Usage

<ComponentDetails componentName="NativeTextArea" templateName="usage" />

## Accessibility

<ComponentDetails componentName="NativeTextArea" templateName="a11y" />

## Props

### accessibilityHint <Badge variant="web" />

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="NativeTextArea"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-accessibilityHint" />

### accessibilityLabel <Badge variant="web" />

Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="NativeTextArea"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-accessibilityLabel" />

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="NativeTextArea"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-accessibilityLabelledBy" />

### compact <Badge variant="web" />

Enables compact variation

<ApiDetails
  componentName="NativeTextArea"
  propertyName="compact"
  required={false}
  defaultValue="false"
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-compact" />

### containerSpacing <Badge variant="web" />

Custom container spacing if needed. This will add to the existing spacing

<ApiDetails
  componentName="NativeTextArea"
  propertyName="containerSpacing"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-containerSpacing" />

### onPress <Badge variant="web" />

Callback fired when pressed/clicked

<ApiDetails
  componentName="NativeTextArea"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-onPress" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="NativeTextArea"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NativeTextArea" templateName="prop-testID" />

<ComponentDetails componentName="NativeTextArea" templateName="outro" />
