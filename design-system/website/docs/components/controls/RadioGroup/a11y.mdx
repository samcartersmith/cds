Radios should not be used alone because it is for selecting one option from a set. The RadioGroup should have a meaningful label describing what the choices are for. Label can be provided as a prop `label` to `RadioGroup` or associated via `aria-labelledby` If there are more than one radio groups on the screen, be sure to provide name for each radio group so that they can have their own focus.
