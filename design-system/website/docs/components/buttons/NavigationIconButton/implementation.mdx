import { ApiDetails } from ':cds-website/components/ApiDetails';
import { Badge } from ':cds-website/components/Badge';
import { ComponentDetails } from ':cds-website/components/ComponentDetails';
import { RightSidebar } from ':cds-website/src/theme/RightSidebar';

<RightSidebar toc={toc} />

> [@cbhq/cds-web/buttons/NavigationIconButton](https://github.cbhq.net/mono/repo/blob/master/eng/shared/design-system/web/buttons/NavigationIconButton.tsx)

<ComponentDetails componentName="NavigationIconButton" templateName="intro" />

## Usage

<ComponentDetails componentName="NavigationIconButton" templateName="usage" />

## Accessibility

<ComponentDetails componentName="NavigationIconButton" templateName="a11y" />

## Props

### accessibilityDescriptionId <Badge variant="web" />

Used to associate descriptive text to an input for accessibility

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="accessibilityDescriptionId"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails
  componentName="NavigationIconButton"
  templateName="prop-accessibilityDescriptionId"
/>

### accessibilityHint <Badge variant="web" />

Accessibility hint for assisted devices when focused. Message should indicate the purpose of the control. If not supplied, will default to label

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="accessibilityHint"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-accessibilityHint" />

### accessibilityLabel <Badge variant="web" />

Full length accessibility label when the child text is not descriptive enough. Accessibility label describing the control. If no accessibilityLabel, it will set it to label Maps to `aria-label`

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="accessibilityLabel"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-accessibilityLabel" />

### accessibilityLabelId <Badge variant="web" />

Used to associate a label with an input for accessibility

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="accessibilityLabelId"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-accessibilityLabelId" />

### accessibilityLabelledBy <Badge variant="web" />

Accessibility prop that describes the component that labels it by its `id`

**https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-labelledby_attribute**

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="accessibilityLabelledBy"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails
  componentName="NavigationIconButton"
  templateName="prop-accessibilityLabelledBy"
/>

### active <Badge variant="web" />

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="active"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-active" />

### as <Badge variant="web" />

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="as"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'ComponentClass<HTMLAttributes<HTMLElement>, any>',
    'FunctionComponent<HTMLAttributes<HTMLElement>>',
  ]}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-as" />

### compact <Badge variant="web" />

Reduce the inner padding within the button itself.

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="compact"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-compact" />

### disabled <Badge variant="web" />

Mark the button as disabled.

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="disabled"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-disabled" />

### loading <Badge variant="web" />

Is the element currenty loading.

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="loading"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['false', 'true']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-loading" />

### name <Badge variant="web" />

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="name"
  required={true}
  defaultValue=""
  mobileOptions={[]}
  webOptions={[
    'account',
    'addPeople',
    'appSwitcher',
    'application',
    'bell',
    'caretUp',
    'cash',
    'chartBar',
    'chartCandles',
    'chartPie',
    'dashboard',
    'defi',
    'directDeposit',
    'documentation',
    'earn',
    'eye',
    'gab',
    'giftBox',
    'home',
    'invoice',
    'moreVertical',
    'newsfeed',
    'pay',
    'paymentCard',
    'planet',
    'scanQrCode',
    'securityShield',
    'settings',
    'shoppingCart',
    'support',
    'tag',
    'taxes',
    'trading',
    'trophyCup',
    'wallet',
  ]}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-name" />

### onKeyPress <Badge variant="web" />

Callback fired when a key is pressed

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="onKeyPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['KeyboardEventHandler<Element> & KeyboardEventHandler<HTMLButtonElement>']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-onKeyPress" />

### onPress <Badge variant="web" />

Callback fired when the element is pressed.

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="onPress"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['MouseEventHandler<Element>']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-onPress" />

### tabIndex <Badge variant="web" />

Necessary to control roving tabindex for accessibility https://www.w3.org/TR/wai-aria-practices/#kbd_roving_tabindex

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="tabIndex"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['number']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-tabIndex" />

### testID <Badge variant="web" />

Used to locate this element in unit and end-to-end tests. Under the hood, testID translates to data-testid on Web. On Mobile, testID stays the same - testID

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="testID"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-testID" />

### to <Badge variant="web" />

URL that this links to when pressed.

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="to"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['string']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-to" />

### variant <Badge variant="web" />

<ApiDetails
  componentName="NavigationIconButton"
  propertyName="variant"
  required={false}
  defaultValue=""
  mobileOptions={[]}
  webOptions={['Omit<IconButtonVariant, primary>']}
/>

<ComponentDetails componentName="NavigationIconButton" templateName="prop-variant" />

<ComponentDetails componentName="NavigationIconButton" templateName="outro" />
