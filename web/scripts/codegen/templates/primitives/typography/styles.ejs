import { css } from 'linaria';

<% Object.entries(styleObjects).forEach(([name, styles ]) => { %>
    export const <%- name %> = css`
        <%_ Object.entries(styles).map(([attr, value]) => { _%>
            <%- attr %>: <%- value %>;
        <%_ }) _%>
    `;
<% }) %>