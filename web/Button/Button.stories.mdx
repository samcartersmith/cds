import { withStoryLabel } from '@cds/storybook/decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import { Button } from './Button';

<Meta title="Core Components/Button" component={Button} argTypes={{}} />

# Button

<Canvas isColumn withToolbar>
  <Story name="Default" decorators={[withStoryLabel('Default')]} args={{}}>
    {args => (
      <div>
        <Button
          {...args}
          onPress={console.log}
          onPressStart={console.log}
          onPressEnd={console.log}
          onPressChange={console.log}
          onPressUp={console.log}
        >
          Button
        </Button>
      </div>
    )}
  </Story>
  <Story name="Compact" decorators={[withStoryLabel('Compact')]} args={{ compact: true }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>
      </div>
    )}
  </Story>
  <Story name="Block" decorators={[withStoryLabel('Block')]} args={{ block: true }}>
    {args => (
      <div>
        <Button {...args}>Full-width button</Button>
      </div>
    )}
  </Story>
  <Story
    name="Block compact"
    decorators={[withStoryLabel('Block compact')]}
    args={{ block: true, compact: true }}
  >
    {args => (
      <div>
        <Button {...args}>Full-width button</Button>
      </div>
    )}
  </Story>
</Canvas>

# States

<Canvas withToolbar>
  <Story name="Loading" decorators={[withStoryLabel('Loading')]} args={{ loading: true }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>
      </div>
    )}
  </Story>
  <Story name="Disabled" decorators={[withStoryLabel('Disabled')]} args={{ disabled: true }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>
      </div>
    )}
  </Story>
</Canvas>

# Variants

<Canvas withToolbar>
  <Story name="Primary" decorators={[withStoryLabel('Primary')]} args={{ variant: 'primary' }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>{' '}
        <Button {...args} compact>
          Button
        </Button>
      </div>
    )}
  </Story>
  <Story
    name="Secondary"
    decorators={[withStoryLabel('Secondary')]}
    args={{ variant: 'secondary' }}
  >
    {args => (
      <div>
        <Button {...args}>Button</Button>{' '}
        <Button {...args} compact>
          Button
        </Button>
      </div>
    )}
  </Story>
  <Story name="Positive" decorators={[withStoryLabel('Positive')]} args={{ variant: 'positive' }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>{' '}
        <Button {...args} compact>
          Button
        </Button>
      </div>
    )}
  </Story>
  <Story name="Negative" decorators={[withStoryLabel('Negative')]} args={{ variant: 'negative' }}>
    {args => (
      <div>
        <Button {...args}>Button</Button>{' '}
        <Button {...args} compact>
          Button
        </Button>
      </div>
    )}
  </Story>
</Canvas>

## API

<ArgsTable />
