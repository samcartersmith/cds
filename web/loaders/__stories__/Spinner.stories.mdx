import { paletteForegrounds } from '@cbhq/cds-common';
import { withStoryLabel } from '@cbhq/cds-storybook/decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';

import { Spinner } from '../Spinner';

<Meta title="Core Components/Loaders/Spinner" component={Spinner} />

# Spinners

<Canvas withToolbar>
  <Story
    name="Spinner Default"
    decorators={[withStoryLabel('Spinner Default')]}
    args={{
      size: 10,
    }}
    argTypes={{
      color: {
        control: {
          type: 'select',
          options: paletteForegrounds,
        },
      },
    }}
  >
    {args => <Spinner {...args} />}
  </Story>
  <Story
    name="Spinner Primary"
    decorators={[withStoryLabel('Spinner Primary')]}
    args={{
      size: 10,
      color: 'primary',
    }}
    argTypes={{
      color: {
        control: {
          type: 'select',
          options: paletteForegrounds,
        },
      },
    }}
  >
    {args => <Spinner {...args} />}
  </Story>
</Canvas>

### Spinner API

<ArgsTable of={Spinner} />
