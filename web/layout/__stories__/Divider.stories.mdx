import { withStoryLabel } from '@cbhq/cds-storybook/decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';

import { Box } from '../Box';
import { Divider } from '../Divider';
import { HStack } from '../HStack';

<Meta title="Core Components/Divider" component={Divider} />

# Divider

<Canvas withToolbar>
  <Story
    name="Horizontal direction"
    decorators={[withStoryLabel('Horizontal')]}
    args={{ direction: 'horizontal' }}
  >
    {args => <Divider {...args} />}
  </Story>
  <Story
    name="Vertical direction"
    decorators={[withStoryLabel('Vertical')]}
    args={{ direction: 'vertical' }}
  >
    {args => (
      <div style={{ height: 100, display: 'flex' }}>
        <Divider {...args} />
      </div>
    )}
  </Story>
</Canvas>

# Colors

<Canvas withToolbar>
  <Story name="Light color" decorators={[withStoryLabel('Light')]} args={{ color: 'line' }}>
    {args => <Divider {...args} />}
  </Story>
  <Story
    name="Heavy color"
    decorators={[withStoryLabel('Heavy')]}
    args={{ direction: 'vertical', color: 'lineHeavy' }}
  >
    {args => (
      <HStack>
        <Box height={100} width={100} background="backgroundAlternate" />
        <Divider {...args} />
        <Box height={100} width={100} background="backgroundAlternate" />
      </HStack>
    )}
  </Story>
</Canvas>

## API

<ArgsTable />
