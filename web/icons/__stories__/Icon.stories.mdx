import { withStoryLabel } from '@cbhq/cds-storybook/decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import { HStack } from '../../layout';
import { TextLabel1 } from '../../typography';
import { Icon } from '../Icon';
import { iconName } from './iconData';

<Meta title="Core Components/Icon" component={Icon} />

# Icon

<Canvas isColumn withToolbar>
  <Story name="sizes" decorators={[withStoryLabel('sizes')]}>
    <HStack flexWrap="wrap">
      {iconName.map(name => (
        <div key={name}>
          <Icon name={name} title={name} spacing={7} color="foreground" size="xs" />
          <TextLabel1 align="center" as="p">
            {name}
          </TextLabel1>
        </div>
      ))}
      {iconName.map(name => (
        <div key={name}>
          <Icon name={name} title={name} spacing={7} color="foreground" size="s" />
          <TextLabel1 align="center" as="p">
            {name}
          </TextLabel1>
        </div>
      ))}
      {iconName.map(name => (
        <div key={name}>
          <Icon name={name} title={name} spacing={7} color="foreground" size="m" />
          <TextLabel1 align="center" as="p">
            {name}
          </TextLabel1>
        </div>
      ))}
      {iconName.map(name => (
        <div key={name}>
          <Icon name={name} title={name} spacing={7} color="foreground" size="l" />
          <TextLabel1 align="center" as="p">
            {name}
          </TextLabel1>
        </div>
      ))}
    </HStack>
  </Story>
</Canvas>

## API

<ArgsTable />
