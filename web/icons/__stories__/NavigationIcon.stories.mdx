import { withStoryLabel } from '@cbhq/cds-storybook/decorators/withStoryLabel';
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import { Box } from '../../layout/Box';
import { HStack } from '../../layout/HStack';
import { TextLabel1 } from '../../typography';
import { NavigationIcon } from '../NavigationIcon';
import { navigationIconNames } from './iconData';

<Meta title="Core Components/Icon/NavigationIcon" component={NavigationIcon} />

# NavigationIcon

<Canvas isColumn withToolbar>
  <Story name="sizes" decorators={[withStoryLabel('sizes')]}>
    <div>
      <Box spacing={2}>
        <TextLabel1 color="primary" as="p">
          Size: XS (Inactive)
        </TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} title={name} size="xs" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: XS (Active)</TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} active title={name} size="xs" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: S (Inactive)</TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} title={name} size="s" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: S (Active)</TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon active name={name} title={name} size="s" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: M (Inactive) </TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} title={name} size="m" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: M (Active) </TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} active title={name} size="m" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: L (Inactive)</TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon name={name} title={name} size="l" spacing={7} />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
    <div>
      <Box spacing={2}>
        <TextLabel1 as="p">Size: L (Active)</TextLabel1>
      </Box>
      <HStack flexWrap="wrap">
        {navigationIconNames.map(name => (
          <div key={name}>
            <NavigationIcon
              name={name}
              active
              title={name}
              labelledby={name}
              testID="navigationIcon"
              size="l"
              spacing={7}
            />
            <TextLabel1 align="center" as="p">
              {name}
            </TextLabel1>
          </div>
        ))}
      </HStack>
    </div>
  </Story>
</Canvas>

## API

<ArgsTable />
