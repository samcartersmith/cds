import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  progressLabelContainer: {
    position: 'relative',
    height: 20,
  },
  labelTextStyle: {
    position: 'absolute',
  },
});
