import { opacityDisabled } from '@cbhq/cds-common/tokens/interactableOpacity';
import { StyleSheet } from 'react-native';

export const interactableStyles = StyleSheet.create({
  disabled: {
    opacity: opacityDisabled,
  },
});
