name: CI
on:
  push:
    branches: master
  pull_request:
    branches: master

concurrency:
  group: CI-${{github.ref_name}}-${{github.event.pull_request.number || github.sha}}
  cancel-in-progress: ${{github.event_name == 'pull_request'}}

env:
  CI: true
  PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1

jobs:
  lint:
    name: Lint
    runs-on: [large, amd64]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100 # TODO: This needs to include the merge-base
      - uses: ./.github/actions/setup
      - name: Lint (All)
        if: ${{ github.event_name == 'push' }}
        run: yarn nx run-many --target=lint
      - name: Lint (Affected)
        if: ${{ github.event_name == 'pull_request' }}
        run: yarn nx affected --target=lint

  format:
    name: Format
    runs-on: [medium, amd64]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100 # TODO: This needs to include the merge-base
      - uses: ./.github/actions/setup
      - name: Format (All)
        if: ${{ github.event_name == 'push' }}
        run: yarn nx format:check  --verbose --all
      - name: Format (Affected)
        if: ${{ github.event_name == 'pull_request' }}
        run: yarn nx format:check --verbose

  test:
    name: Test
    runs-on: [xlarge, amd64]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100 # TODO: This needs to include the merge-base
      - uses: ./.github/actions/setup
      - name: Test (All)
        if: ${{ github.event_name == 'push' }}
        run: |
          yarn nx run-many --target=test
          yarn nx run-many --target=audit-a11y
      - name: Test (Affected)
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          yarn nx affected --target=test
          yarn nx affected --target=audit-a11y

  storybook-a11y:
    if: ${{ github.event_name == 'pull_request' }}
    name: Storybook a11y
    runs-on: [medium, amd64]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100 # TODO: This needs to include the merge-base
      - uses: ./.github/actions/setup
      - uses: infra/cb-action-docker@master
        with:
          image: 652969937640.dkr.ecr.us-east-1.amazonaws.com/containers/node:v20-chrome-ub22
          command: |-
            yarn nx affected --target=a11y-web

  storybook-e2e:
    if: ${{ github.event_name == 'pull_request' }}
    name: Storybook E2E
    runs-on: [medium, amd64]
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 0
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100 # TODO: This needs to include the merge-base
      - uses: ./.github/actions/setup
      - uses: infra/cb-action-docker@master
        with:
          image: 652969937640.dkr.ecr.us-east-1.amazonaws.com/containers/node:v20-chrome-ub22
          command: |-
            npx playwright install --with-deps
            yarn nx affected --target=storybook-e2e-test
