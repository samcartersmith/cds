# Auto-generated workflow for repository synchronization
# This workflow keeps this repository in sync between GHES and GHEC
# Generated by: ghes-to-ghec-migrator/setup-repo-sync workflow

name: Sync Repository

on:
  push:
    branches: ['master', 'main']
  workflow_dispatch:

jobs:
  sync:
    uses: infra/ghes-to-ghec-migrator/.github/workflows/repo-sync-ghes-to-ghec.yml@main
    with:
      source_repo: ${{ github.repository }}
      branches_to_sync: 'master,main'
      sync_tags: true
